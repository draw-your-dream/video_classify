
**尾巴/眉毛探针补充判决(2026-08-06 16:5x-17:1x)**:
尾巴三连:①锚定问法(写明粉色/左臀线索)= 暗示性点头,负对照7060同报"粉色左臀凸起",不可用;
②中性+448全身 = 漏(凸起~15px);③**中性+下身60%×2放大 = 自发报出"浅粉色@臀部两侧"且双负对照干净**——
配方定型:放大给召回,中性问法保精度,线索绝不入prompt。
眉眼合并锚测:召回5/5(眉3靶+尾靶+正对照),但窄眼负对照2/2误报(6472在单独问眉时干净,
加眼睛问题后翻车=多问题分散注意)。合并版15.5s/条(全库21h)。
**用户指令:VLM全量挂起,转跑僵硬特征**。
**僵硬特征电池预注册(2026-08-06 17:0x,四族运动-形变耦合,非VLM)**:
F1刚体拟合残差(RAFT角色框相似变换,rr_mean/p25/lowfrac+象限分歧) F2事件对齐形变
(bbox面积/长宽比变化×能量耦合) F3节奏(逐帧差能量全帧率:起步上升时间/峰度/静止段高频/acf)
F4跟随滞后(上1/3vs下2/3条带互相关滞后)。15列全语料,冒烟过(坐标系bug已修:sam3 bbox=原始系)。
后续:僵硬专用头(581僵硬vs good,特征电池⊕320,超参独立搜索,对标E24组内AUC 0.840)+
高精度阈值报告器名单交用户审。

**全特征化战役发车(2026-08-06 17:2x,用户指令:全部上GPU尝试包括眉毛,VLM全量继续挂起)**:
①僵硬 rigid_feats 15列已全量落地(4845行,nan 2%,0.16s/条);
②尾巴 tail_pink 预注册:sam3_cutouts白底抠像,掩码下62%内粉色连通块(排脸颊),
7列(persist/ratio/贴边率/位置稳定/hue);校准锚=基础款5822须高分;CPU全量;
③眉毛 brow_distill 预注册:VLM旗标蒸馏帧级CNN(efficientnet_b0),
正=v4∪v5旗标帧(上62%放大),负=零旗标good抽帧;**七锚全部排除出训练只做检验**
(H086/089/101须高分=蒸馏泛化超出教师;7241/6472低分=不复制教师窄眼误报;5822低分);
留出帧AUC同报。后续:僵硬专用头训练(本地)+三族特征汇总判决。

**E30 判决(2026-08-06 19:5x):僵硬信号结合总分四路线全灭,根因判明**。
基准0.3218:B交互5列 0.3143 | C僵硬头单列 0.3196 | D秩融合最好0.3165(λ↑单调恶化) |
A带新特征60trial重搜索 0.3072(选中28叶大模型,过拟合OOF反降)。
**决定性证据:僵硬头top60(精度88%)∩ 82条尾部bad = 0**——报告器抓到的53条真僵硬
全是模型已高分的"易僵硬",而卡阈值的32条尾部僵硬bad在报告器里一条不占。
结论:僵硬信号与总分的关系=同一信号的两次采样(易僵硬两边都能抓,难僵硬两边都盲),
不是互补信号。放行率与僵硬报告器是两个独立战场;尾部僵硬的判别信息不在现有任何
表征里(第5次复现),唯一确定路径=补标注加厚尾部簇。

**E30-eval 预注册(2026-08-06 20:2x,用户指令门外单发)**:四路线均未过训练门,用户要求
看eval;只发 OOF 最高的 C 路线(僵硬头OOF单列并入冠军,train 0.3196 vs 基准0.3218)。
单发一次,对照 E18 冠军 eval 0.2913;如实入账,不论涨跌。

**E45 批次3/4 + E46 判决(2026-08-09)。32B 是唯一过尾部门者**。fold0,覆盖 1429/1938:
| 配置 | 整体 AUC | **tail-AUC** | 判决 |
|---|---|---|---|
| **32B + 三档** | 0.6587 | **0.5727** | **✔ 唯一过门** |
| 8B + 三档 + 多任务缺陷词 | 0.6564 | 0.5230 | ✘ |
| 8B + 三档 + 尾部加权 α=2.0 | 0.6530 | 0.5289 | ✘ |
| 8B + 三档 | 0.6529 | 0.5242 | ✘ |
| 8B + 二分 | 0.6464 | 0.5155 | ✘ |
| E46 尾部专训 balanced | 0.5162 | 0.5324 | ✘ 过拟合 |
| E46 尾部专训 hard_neg | **0.4667** | 0.4951 | ✘ 过拟合更重 |

**撤回一条此前的过度解读**:先前据"我们的纯二分 0.6464 < E36 fold0 0.677"推测多任务缺陷词值
约 +3pt,现直接测得**仅 +0.35pt**(0.6564 vs 0.6529)。真实解释是 fold 间波动——E36 自报
full 0.649 / fold0 0.677 相差 2.8pt,0.677 是该折的运气;我们的结果与其 full 0.649 一致。
**教训:不可从跨实验的单折数字反推某个设计元素的效应量,必须直接消融。**

**E46 判决:按预注册的判读规则判为过拟合,非真信号**。发车前已写死"整体 AUC 大幅塌陷而
tail-AUC 虚高即判过拟合",本次正中该模式:训练集 1938 → **316 条**(158 正 + 158 负),
3 epoch 后 loss 降至 0.09,**整体 AUC 塌至 0.4667(劣于随机)**。
`hard_neg` 比 `balanced` 更惨(0.4667 vs 0.5162):其负样本取自 E18 分数 0.585~0.889 的
可放行样本,与 tail bad 在表征上本就极难分,用它们当训练集只能学到噪声。
**结论:"只在最差样本上训练"在 VLM 层被样本量卡死(316 条),不是思路错**;
若要复活须先解掉样本量约束(冻结更多层 / 只训轻量头 / 放宽 tail-frac)。

**8B 侧的稳定性是硬证据**:五个 8B 配置(换目标格式、改训练分布、加文本信号)的 tail-AUC
落在 **0.5155~0.5324** 的极窄带内。改监督信号无法突破,说明瓶颈在表征而非目标——
而 32B 把它抬到 0.5727,**唯一起作用的维度是容量**。

**16 帧未得结论**:与 32B 并行时争显存 OOM(32B 占 45.5GB),非配置问题,已重跑。
**下一步(按预注册)**:32B 过门 → 跑满折 OOF(已发车)→ 入栈验 train-OOF gn@95 > 0.3218。
须记录的方法学限制:32B 用 2 折 OOF 而组合器用 5 折,折结构不一致会带来轻微乐观偏差,
入栈判读时须计入。

**E51 判准① 终判(2026-08-11 晚):两轴皆未过,运动族"软硬+面部"特征线收档**。
真 face_crops(SAM3 原生 384px,全语料 4845)上:face_act MT|G 反向 0.571 / MB|G 0.643,
face_act_p90 与 blink 归零(≈0.50)。**试点的 0.775/0.776 确认为 919 保真子集的选择偏差**
(该子集的 bad 以保真缺陷为主,其“脸死”与全语料运动族不同构)。
轮廓软度 0.648/0.683 亦未达 0.70。判准①两轴皆败,组合判别(v2/v3)否决模拟已先行判负。
**E56 判决(用户指令:眉毛入栈)**:+0.49pt(5种子均值 0.3204 vs 0.3155,4/5 胜但 seed42 反向)
——低于 E42 确立的可验证分辨率(+0.78pt train显著仍 eval 归零),且否决门已确定性兑现同一信号,
入栈会重复计算。**不晋级 eval,眉毛信号的部署形态维持否决门。**
工程事故记录:sam3 pip 安装降级了 transformers,致 E53/E55 首次发车崩
(AutoModelForImageTextToText 消失);已固定 transformers==5.14.1 重发。

**E53 判决(2026-08-11 夜):VLM 成对比较亦不能判活物感——运动族方法面全谱收档**。
240 对比较(80 候选盲序 × 3 参照,位置交替):运动尾部bad 平均输票 **2.00**/3 vs good **1.77**/3,
差 **0.23 << 1.0 判准**;good 高输票率 **59% >> 20% 判准**。双判准皆大幅未过。
判读:32B 连成对形态下都无法区分"僵硬 bad"与普通 good 的活物感——good 也在参照面前大面积
被判"不够活"(参照为高置信+高运动量 good,但差分才是判据,差分≈0)。
**运动族(11/20 尾部,最大头寸)至此在方法面全谱关闭**,判决链条:
特征三代(E30 光流 0.66 上限 / E51 轮廓软度 0.68 / E52 卡顿指纹 0.66)→ 组合判别 0.744(区内)
→ 否决模拟三形态全负(v2 全域 / v3 区内 / 单特征极端)→ 入栈 E54 零增益 →
VLM 四范式全灭(E4 绝对打分 0.546 / E50-D 事实提问 差31pt / E45 微调 0.52 带 / E53 成对 差0.23)。
**共同根因(三次独立复现):好视频里的 TUTU 也大量呈现低形变、低面部活动、静止**
——"僵硬"作为人类判断是相对预期的 gestalt,不是当前任何单模态可测物理量的阈值事件。
剩余通路仅数据侧:①产线域/缺陷子类标注 ②i2v 首帧原图 ③标签复核(13 条清单已交)。
