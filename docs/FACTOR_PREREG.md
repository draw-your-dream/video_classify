# 因子预注册清单(2026-07-28,基于 FAILURE_CATALOG.md)

**范围裁定(2026-07-29,用户决定):只保留 F1-F4 推进;F5/F6/F7 暂缓不用。**
**测量族裁定(2026-07-29):base-224 流水线全量评估显示 F4 有效(15.0%)、F1-F3 未达标;
c_first_last 的 so400m 原版配置远强于 base-224 复刻(26.4% vs 9.7%)——F1-F3 改用 so400m 测量族
(so400m-384 pooler + 方形裁剪,与 crop_feat_original.py 同族)在 H100 重做后再判。**

铁律:因子由失败模式目录先验设计,在此登记后才允许在 prod500 上评估;不许按 27 条上的表现回头挑因子。
评估协议:域内分位 + OR 门;LOO(留一 bad)+ bootstrap 汇报;采纳标准 = ①机制验证通过 ②唯一覆盖 ≥2 条现有轴看不清的 bad ③LOO 下不降低现有门的放行率。
现有基线:dim_fidelity ⊕ c_first_last,27/27 召回,放行 26.4% good(LOO 漏 1/27)。

## 本地可跑(5090,GroundingDINO 每视频 16 帧 + SigLIP2 + OpenCV)

**F1 cap_drift** — 菌盖区域(角色框上 40%)首末帧 SigLIP2 余弦 + 颜色直方图距离。
靶:白点消融/盖形漂移/滑落吞头(64ee, 4c5f, 32da, 5155, 1f65, b729, …≥8 条)。
机制关:语料 skus 还原度 bad vs good 该因子应有分离。

**F2 anchor_min** — min over t of cos(crop_F0, crop_Ft),16 帧。
靶:纯中段漂移(a400 首末均正常的确定性反例);严格强于 first_last。
机制关:在 a400 型样本上应显著低于 first_last 型。

**F3 face_drift** — 脸部区域(角色框中带)F1 同构因子。
靶:凭空长嘴/五官增减(b729, 32da, 5155, 159a, 939c)。

**F4 size_trend** — 角色框高度随时间的趋势/极差,经背景 ORB 单应尺度补偿(排除镜头推拉混淆)。
靶:C 类(78d4, 5155, 5761, e175)。
风险:镜头运动补偿失败时误报;机制关先在有镜头推进的 good 上验证补偿有效。

**F5 nonchar_motion** — 相机运动补偿后,角色框外静物区域的残余光流能量。
靶:I 类(4f95, bd7a, 7a25, 5761)+ B 类滑移。
风险:场景里合法运动主体(人/猫)需检测剔除,否则误报;机制关在含人/宠物的 good 上校准。

## 需短租 GPU(Qwen3-VL-30B,四个定向问题轴,logit(是)−logit(否))

**F6a 悬空** — "这个毛毡玩偶此刻悬在空中/没有站在任何表面上吗?"(B 类)
**F6b 自主运动** — "画面里有没有物体在没有外力的情况下自行移动或变形?"(I 类)
**F6c 静态不可能** — "画面里是否存在物理上不可能的情形:错误的倒影、平面图画有真实深度、不可能维持的平衡?"(D 类——时间信号原理性盲区,唯一出路)
**F6d 凭空出现** — "视频中是否有物体凭空出现或消失?"(F/G 类)
对照:此前笼统问"是否违反物理规律"AUC 0.588;假设 = 定向问题显著更强。

**F7 IP 模板符合度** — prompt 内写死 IP 规范(矮胖短腿、圆头、菌盖带白点戴头顶、毛毡质感),问偏离。
靶:整段偏离型 A(15bf, 8fdc, 159a, 57 61)——首末自比对这类无效,模板写进 prompt = 无域依赖的绝对参照。

## 已知覆盖缺口
- E 类(交互融合,9c51)本轮无专用因子,依赖 F3/F6 兜底;
- 2a8b(H 类)任何因子都不应因它设阈——按 GT 争议处理,若它设阈则单独上报。

## P1 单类 patch 异常(2026-07-30 预注册,语料机制验证先行)

**动机(第一性原理)**:身份属性(拓扑件数/部件位置/质地/配色)不可变,状态属性(姿态/视角/遮挡)自由变。
good 集合定义角色在合法变换下的"轨道",判定 = 是否落在轨道外。缺陷清单映射:
黑点/不合理嘴 = 局部纹理异常;多余肢体/尾巴 = 多余部件(good 库中无近邻);
手脚位置 = 空间关系(需位置条件化,本轮暂不做);身体过长 = 全局几何(patch 盲区,另设比例因子)。
遮挡假警的第一性消歧 = 时间持续性(真缺陷跨帧存在,遮挡只在姿态转换几帧)。

**协议(冻结,先登记后评估)**:
- 提取:16 帧,GroundingDINO 三连 prompt thr 0.25 + 方形 1.25 倍裁剪(crop_feat_original 同款),
  DINOv2-base 518×518 patch tokens(37×37×768),前景 = patch 中心落在检测框内。
- 划分:语料 good 455 以 seed=0 置换,前 300 建库 / 后 155 评估;bad_huanyuandu 464 全评。泄漏隔离。
- 库:bank-good 检出帧前景 patch,每视频至多 1300(md5(rel) 种子确定性子采样)。
- 打分:patch 异常 = 1 − max cos sim(1-NN);帧分 = 前景 top-1%/5% 均值;
  视频分 = 帧分 max / p75 / mean(p75 = 持续性口径)。
- 主指标:bad vs eval-good AUC(fg 口径);bg 口径仅对照。
- **晋级关**:AUC ≥ 0.70 且 fg > bg 口径 → 允许迁移 prod500(库改用 prod good 重建,阈值走分位协议,
  n=27 上不调任何超参);不达标 → 记录后归档,不进门。
- 已知盲区:身体过长/比例类(patch 局部正常)——若语料 bad 中该类占比高,AUC 上限受压,分错误类复盘。

脚本:`extract_patch_feat.py`(特征)/ `patch_bank_eval.py`(建库+打分+AUC)。

### P1 v2 修订(2026-07-30,业界调研后,运行前冻结)

调研依据(要点):
- **AnomalyDINO(WACV'25)**:DINOv2 patch + 1-NN 记忆库为少样本工业检测 SOTA(MVTec 1-shot 96.6 AUROC);
  图级分 = **top-1% patch 距离均值**(tail-value-at-risk),余弦距离——与本协议帧分口径一致,予以保留;
  其 PCA 前景掩码在"对象占画面 ≥50%(close-up)"时失效——我们的裁剪即 close-up,
  故弃 PCA 掩码,用检测框前景掩码(本协议原设计),此为有据取舍。
- **MVTec LOCO / CSAD(BMVC'24)**:缺陷分结构性(纹理/污染)与逻辑性(多余/缺失部件、数量、位置)两类,
  patch-kNN 只覆盖结构性;CSAD 用组件分割+patch 直方图补逻辑性(LOCO 95.3)。
  本轮增设 **s_hist 直方图分支**(bank patch 球面 k-means k=64,帧前景聚类占比 1-NN L1)
  作为逻辑异常的简化探针,靶多余肢体/部件占比失衡。
- **VBench(CVPR'24)**:subject consistency 即用 DINO 特征跨帧余弦——DINO 系对"同类个体差异"敏感,
  适合形象还原判定,支持本轮骨干选择。

修订清单:
1. good 划分改**款分层**(每款按 300/455 比例进库,防某款缺席库导致整款评估误报);seed 确定性不变。
2. 增设 s_hist 逻辑异常分支(同样三口径聚合),与结构分支同表评估。
3. 增加**裁剪对象核验**步骤(verify_crops.py):so400m 零样本分类裁剪(蘑菇角色 vs 4 干扰类),
   win_rate<0.5 进 flag 清单人工复核;并出 款x标签 蒙太奇大图供人工过目——核验不过的视频不参与结论。
4. 晋级关不变:AUC >= 0.70 且 fg > bg;s_hist 单独看,不与结构分支合并调参。

### P1 判决(2026-07-30 深夜,语料 919 机制验证完成)

**未过晋级关,归档不进门。** 全口径 AUC 表(bad 464 vs eval-good 156,款分层划分,泄漏隔离):
最好 s_hist_mean=0.568,结构分支最好 s_top5_fg_mean=0.543;fg 全面>bg(方向对,幅度不足);
剔除跑偏裁剪敏感性三档 0.564–0.571,判决稳健。

**失败机理(核心产出)**:bank_good 分数 P50=0.19 vs eval_good P50=0.42——
开放内容域中每条新视频自带合法新内容(道具/场景/配饰),novelty 噪声地板(0.42)
淹没缺陷信号(bad P50=0.424)。MVTec 范式的封闭场景前提在本域不成立。

**分款分库对照**:池化 0.574(vs 全局库 0.568),款级收紧无效——噪声在款内。
个别款样本小、方差大(花花 top1_fg 0.911@n_good=12,冰晶 hist 0.750,甜品 hist 0.373 反向),不过度解读。
推论:参照系必须收紧到"本视频自身"——**i2v 首帧原图升为头号数据请求**
(自参照 dense 匹配,理论上消灭 novelty 地板)。

**候选因子登记 P2 = 角色在场性(1 − cos_centroid)**:视频前景嵌入到全体中心的余弦,
语料域内单轴 AUC 0.584,专抓角色缺席/被道具顶替;prod500 可用现存裁剪特征直接复算,待预注册评估。

附:v1 零样本核验(so400m 文本塔)在 transformers 5.x 下损坏产生 913/919 假 flag,已作废;
v2 图像侧离心度核验替代,人工蒙太奇复核确认裁剪质量整体良好(检出率 100%,跑偏尾部 ~20 条)。

### 第0步 逐簇 AUC 分解(2026-07-31 预注册,P1 失败后的部件级诊断)

**问题**:缺陷信号是否集中在特定身体区域(脸/肢体),被全身口径的 novelty 地板掩盖?
**方法**(cluster_auc_decomp.py,零新模型,复用 P1 特征与 64 簇):
patch 异常 d 与 P1 同口径;视频×簇分 = 该簇 patch d 的 top-20% 均值(池化全帧);
簇占比 occ 双向轴;逐簇 AUC 仅在该簇出现(>=5 patch)的视频上算,报双侧覆盖;
每簇 12 样本 patch 蒙太奇(行按 AUC 降序)供人工辨认簇语义。
**判读门(先登记)**:存在簇 AUC>=0.65 且覆盖充分(n_bad>=50, n_good>=30)且蒙太奇
显示对应面部/肢体区域 -> 部件线(线A 面部 bank / 线B 肢体计数)立项依据成立;
全簇 <0.60 -> DINOv2+bank 范式对此域缺陷不敏感,部件线改走测量/合成缺陷监督,不再投 bank 类。
**注意**:多重比较(64 簇)下个别簇会偶然高分,故要求覆盖充分+语义可辨认双条件;
本步为诊断定向,不直接产出可上产线的因子。

### 第0步判决(2026-07-31 15:12,新盒复现+逐簇分解完成)

**0 个达标簇,bank 范式在本域正式关闭。**
64 簇外观轴(top-20%)分布:P50=0.502(=抛硬币)、P90=0.552、最高 c16=0.604(覆盖 170/49 不足,
多重比较下不可信);满覆盖(n>=400/140)42 簇中最高 0.587。占比轴最偏 0.613/0.380,同样不可用。
前置复现:新机全链路(下载->提取->核验->评估)与原机千分位一致(s_hist_mean 0.567 vs 0.568,
verify2 cos 中位 0.874 逐字相同),排除"机器/提取漂移"解释——信号就是不在。

**结论链(三级参照系收紧全部失败,机理一致)**:全身 0.568 -> 分款 0.575 -> 逐簇 0.587,
novelty 地板存在于簇内部——同一部位在不同视频里的合法变化(视角/光照/形变/贴身道具)
仍大于缺陷偏移。DINOv2 patch 袋子 + 好样本库这条范式对本域缺陷不敏感,按预注册判读门,
**部件线不再投入 bank 类方法**,转:
  线C 比例轨迹测量(本地即可,零训练)/ 线B 关键点标注+计数 / 线A 合成缺陷监督(免库)。
i2v 首帧原图仍为头号数据请求(自参照才能杀死簇内 novelty)。
蒙太奇 cluster_montage_parts.jpg 留档待人工过目:确认簇确实分出了部位语义
(区分"信号不在"与"分割失败"两种解释,不改变不立项判决)。

## P3 预注册(2026-08-01):SAM3 掩码底座 + 参考图三轴(先登记后评估)

**数据与资源**:tutu-renders-2026-04 参考图 152 张(基础款关键帧 98 + 六饰品款各 9,
combined-1.5x 自带 SAM3 主体掩码);SAM3 权重经 sha256 与官方比对一致(9999e234…)。
前人图片侧管线(Picaa-Image-IP-Similarity-Eval)已消化:检测→SAM 抠像→白底 224 画布
→LPIPS/DreamSim/so400m 对参考集 max→阈值 0.70。其口径成立前提=查询姿态≈参考姿态,
视频域不成立,故弃 LPIPS,继承 SAM3 抠像+白底画布+max-over-refs。

**底座**:每视频 16 均匀帧 → SAM3 文本 prompt(继承前人 "A fluffy mushroom-like creature
with a light-yellow body and a red cap")→ 全实例掩码/框/分;取最高分实例做白底抠像
448 画布存 jpg;记录逐帧:实例数、掩码面积/高宽、伞宽(掩码上部 40% 最大行宽)、
身高(掩码 bbox 高)。蒙太奇人工核验门照旧(抠像质量先过目再进轴)。

**三轴(语料 464 bad vs 156 eval-good,沿用款分层划分,晋级关 AUC>=0.70)**:
- R 参照相似:帧抠像 vs 本款参考抠像池(饰品款 9 张 + 基础款 98 张并集),
  DreamSim 与 so400m-512 图像塔余弦各自 max-over-refs;视频分 = 帧分 p25 / mean(持续不像)。
  前科:前人视频系 fidelity_sku AUC=0.50;翻案三要素=SAM3 掩码/新参考图/DreamSim,不成则永久关闭。
- G 几何:身高÷伞宽逐帧轨迹;静态口径(视频中位 vs 款内 good 分布)+ 漂移口径(末段/首段比)。
  靶身体拉长/比例失衡;视角混淆用持续性压制。
- C 计数:角色实例数轨迹;>=2 持续 K 帧 = 多角色,检出后消失/翻倍类结构异常单独描述统计。
  肢体级计数(arm/leg/tail prompt)为二期,先不跑(成本×4)。
**V 轴(VLM 判官)**:参考图+帧拼图喂新 VLM 按缺陷清单问答——待 R/G/C 出数后单独预注册。
**迁移**:过关轴按分位协议迁 prod500,与现有 dim_fidelity⊕c_first_last 双轴门做 OR 集成,
在 27/27 全召回口径下报告 good 放行率增量。目标:语料与 prod500 双数据集成果。

### P3 判决(2026-08-01 18:10,R/G/C 三轴完成)

**全部未过晋级关(0.70)。** 语料 bad-vs-eval-good:R 轴最好 r_ds_mean(DreamSim 对参照池
平均异离)=0.568,so400m 口径 0.51;G 轴静态 0.53、漂移 0.47(反向);C 轴 0.46-0.47(反向,
实例计数在语料上无信号,SAM3 实例数好坏视频均极稳定为 1)。
prod500 探索性集成(标注为探索,不入正门):基线 26.4% 精确复现;任一新轴并入 OR 门均降低
放行率(13.7%-21.1%),单轴全召回放行 <=10%——新轴抓不住绑定 bad,只多拦 good。
**方法史总结**:至此 0.57 天花板已横跨四个方法族——patch 记忆库(P1)、分款库、逐簇分解、
SAM3 抠像+DreamSim/so400m 参照相似 + 几何 + 计数。唯一强轴 c_first_last 恰是"本视频自参照"。
结论进一步收敛:款级参照与外观度量已穷尽,出路 = 自参照(i2v 原图)/ 判别式(合成缺陷监督)/
语义判官(VLM)。SAM3 底座(抠像/掩码/计数)保留为基础设施,供后续轴复用。

### V 轴预注册(2026-08-01,VLM 判官试点,学长推荐)

**方法**:Qwen3-VL-8B-Instruct(temperature=0),多图输入 = 本款参考图 1 张 + 视频白底抠像
4 帧(f0/f5/f10/f15);中文按缺陷清单结构化问答(脸部黑点/嘴异常/肢体数异常/尾巴/比例拉长/
整体还原 0-10 分),输出 JSON。主轴 = 10 - 整体还原分(越大越可疑);缺陷布尔为辅描述。
**试点**:语料 bad 抽 120 + eval_good 全 156(确定性种子),报 AUC。
**判读门**:主轴 AUC>=0.70 -> 全量+prod500;0.60-0.70 -> 提示词/帧数迭代一轮再判;
<0.60 -> VLM 判官关闭,收敛到 自参照数据请求+合成缺陷监督 两条路。
**前科声明**:前人 VLM 问答轴(fatalflaw/judgment 等)0.42-0.50,但无参考图对照、模型为旧代;
翻案要素 = 参考图对照 + 抠像输入 + 新代 VLM。

### V 轴判决(2026-08-01 19:35,试点完成)

**AUC=0.500,退化输出,按门(<0.60)关闭。** 261/276 解析成功,bad 113 / good 148 全部
fidelity_score=9、全部缺陷布尔为 false——Qwen3-VL-8B 在参考图对照下对本域缺陷粒度不敏感
(reason 字段显示模型在认真看图:能点出四叶草装饰/遮挡/视角,但从不判缺陷)。
可能后续:更大杯 VLM(72B/GPT级)或逐缺陷单独提问属新配置,需另行预注册评估,今晚不做。

### 2026-08-01 战役总结

一晚测掉三个范式:R/G/C(SAM3+新参考图+DreamSim,语料最好 0.568,prod 集成只降不升)、
V(VLM 判官,0.500 退化)。加上 P1/分款/逐簇,**"款级参照+外观度量+语义判官"全路线已穷尽**,
0.57 天花板横跨五个方法族。存活方向(按证据强度排序):
1. **i2v 首帧原图**(自参照;唯一强轴 c_first_last 即自参照的旁证)——头号数据请求
2. **合成缺陷监督**(线A,判别式,免参照;唯一未测范式)
3. **缺陷子类型标注**——解释 0.57 天花板:怀疑「还原度」标签集异质
   (部分 bad 或为运动类/时序类缺陷,静帧不可见),需子标签才能裁决
基础设施沉淀:SAM3 掩码抠像管线(1.2s/条)、152 张参照池嵌入、白底画布标准化——供后续复用。

### V2 + R-self 预注册(2026-08-01 20:10)

**V2(最强 VLM 判官重测,用户指令)**:judge 换 Claude(headless 强推理),输入=本款参考图+4帧抠像,
10 条/批;试点 60 bad + 60 eval_good(取自 V1 试点集,可直接对比);同一 JSON 判词结构。
判读门:主轴 AUC>=0.70 -> 扩全量并 codex/GPT-5.x 交叉验证;0.60-0.70 -> 提示词迭代一轮;
<0.60 -> V 线(含最强模型)整体关闭。
**R-self(语料自参照轴,c_first_last 的语料对应物,首次测量)**:每视频 f0 抠像为自锚,
so400m-512 嵌入,drift_t = 1 - cos(f_t, f0);视频分 = drift 的 max / p75 / 末段均值 / 斜率。
靶中途形变/长出结构/脸崩(自己离自己越来越远)。晋级关 AUC>=0.70;
0.60-0.70 视为 i2v 原图价值的强化证据(自锚是原图的退化版,原图应更强)。

### V2 判决(2026-08-01 20:40,Qwen3-VL-32B)

排序轴 v_main AUC=0.571 < 0.60,按门**关闭 V 线作为排序轴**(8B 0.500 退化,32B 非退化但同撞天花板)。
**预注册外的登记发现**:32B 旗标零误报低召回——other_defect 7/56 bad 触发、good 0/57;tail 1/56。
与标签异质假设定量吻合:若仅 x 比例 bad 静帧可见,理论 AUC≈0.5+x/2;观测 0.571→x≈14%≈旗标率 12%。
六方法族 0.57 天花板的最简一致解释 = 「还原度 bad 中静帧可见缺陷占比 ~15%」。
**裁决实验(预注册)**:盲审页 = VLM 旗标 bad 7 + 随机未旗标 bad 23 + 随机 eval_good 10,乱序匿名,
用户逐条判「静帧能否看出形象问题」;判读:若未旗标 bad 的可见率 < 30%,标签异质假设成立,
语料还原度轴止损,火力全转 prod 域与 i2v 原图;若可见率高,则方法仍有失败责任,线A 继续主攻。

### R-self 判决(2026-08-01 21:00)

**全口径反向(0.41-0.48),关闭。** good 自漂移 > bad——全局嵌入帧间距离量的是动作幅度,
good 动作更丰富;语料 fidelity bad 是静态外观问题,自漂移不可见。
c_first_last 在 prod 有效而其语料对应物反向,再证语料/产线缺陷构成不同
(prod bad 含形象漂移/帧跳变型,语料还原度 bad 不含)。
推论:i2v 原图的价值主张在语料上无法预演,必须直接在 prod 域验证。

### 线A v0 判决(2026-08-01 21:20)

真实 bad 上 p75/max AUC = 0.518/0.527,未过关。**但仪器自检改写解释**:同一个头在
held-out 合成缺陷(eval_good 帧 ± 合成,训练未见)上帧级 AUC=0.876——
black_spots 0.979 / tail 0.996 / elongate 0.969 / smudge 0.945 / extra_limb 0.920(warp 0.521,
合成过轻,v1 待改)。检测器对用户缺陷清单有效,真 bad 上失败 => 语料还原度 bad 中
静帧可见缺陷占比过低(与 V2 零误报 12% 旗标率、0.57 天花板三方互证)。
**标签异质假设升级为主解释,待盲审(39 例已备)最终裁决。**
线A 资产保留:synth_defects.py 七族合成 + synth_head_v0.pt——若盲审证实 prod 域缺陷静帧可见,
此头可直接迁 prod 评估(合成管线与域无关)。

### V3 预注册(2026-08-01 20:50,用户指令:prompt 迭代)

同模型(Qwen3-VL-32B)同输入,三个 prompt 变体,同 120 试点集(降级为 prompt 开发集):
  V3a 先逐帧描述差异再输出判定 JSON(CoT,max_tokens 放宽到 600)
  V3b 基率校准(明示"本批约半数有缺陷")+ 逐帧 0-100 缺陷概率带锚点,视频分=帧max
  V3c 找不同式:列出与参考图所有不同点,逐条 is_defect+confidence,分=缺陷项最大置信
纪律:三变体全报,任何胜出配置须在**新抽不相交 120 条**上确认 AUC>=0.65 才可晋级;
开发集上的最好成绩不作为结论。

### V3 判决(2026-08-01 21:25)

V3a(CoT)0.598 / V3b(基率校准)0.527 / V3c(找不同)0.579,均<0.65 确认线,V 线收档。
机理:V3b 被基率提示带成反向塌缩(好坏全 70-95),V3c 差异全标缺陷置信 95——
prompt 只移动工作点(保守<->激进),判别力钉在 0.57-0.60 = 静帧可见比例的信息论天花板。
开发集最好成绩 0.598 依预注册不作结论。V 系列完整档案:v1(8B) 0.500 / v2(32B) 0.571 /
v3a-c 0.598/0.527/0.579;裁决权移交盲审。

### 盲审结果记档 + 方向修正(2026-08-01 22:10)

盲审(标注者自评不熟悉产品,作弱证据):未旗标bad可见率14/20=70%,VLM旗标确认7/8,
good误判4/9;"静帧不可见"假设被弱证据反向——bad 的偏离(眉毛/外观)人眼可见而机器漏掉。
用户指令:继续提升方法。修正方向:
- E 集成轴(预注册):既有独立机制轴的**无拟合秩平均**(s_hist_mean + r_ds_mean + g_asp_med,
  等权,不调参),AUC 直接报告;任何加权/选择变体都须新样本确认。
- F 脸部高分辨率专线(预注册):SAM3 "face" prompt 定位脸(蒙太奇核验门)→ 从**原生分辨率帧**
  裁脸 → 线A v1 脸尺度训练,合成族增加眉毛(TUTU标准无眉毛,画眉=缺陷)/五官位移/加强warp;
  评估口径同线A(真实bad,AUC>=0.65 关注、>=0.70 晋级)。

### E 集成轴判决(2026-08-01 22:15)

无拟合秩平均:hist+ds 0.574 / +asp 0.570 / +so 0.556,vs 单轴 0.568——
各族轴高度相关(找到的是同一份弱信号),集成无增量,E 线关闭。

### 线F 判决 + 语料还原度轴收档(2026-08-01 23:05)

线F v1(脸部原生分辨率+五族合成):真实 bad AUC p75=0.529;仪器自检 held-out 合成
**0.974**(eyebrows 1.000 / eye_extra 0.984 / mouth_warp 0.977 / spots 0.971 / smudge 0.951)。
覆盖率满(464/464, 156/156)。
**测量链闭合**:对缺陷清单(含盲审点名的眉毛)灵敏度 0.97+ 的检测器,在真实还原度 bad 上
找不到区分信号。结合全身版(自检 0.876/真实 0.518)与两晚全部方法族:
**语料「还原度」标签不是由清单型静态外观缺陷驱动的**(驱动因素:运动/时序伪影、
整体观感、或标注不一致——静帧口径不可恢复)。
校准声明:结论限于"按清单字面合成的缺陷类型";细微比例偏差类未穷尽,但 DreamSim/patch/VLM
对此类也已测毙。**语料还原度轴正式收档**,不再投入。
火力全转:prod 域(27bad 全召回口径,缺陷构成与语料不同已证)+ i2v 首帧原图 + 向学长要
缺陷子类型标注与更多产线标注。沉淀资产:SAM3 全身/脸部提取管线、身体七族+脸部五族合成、
synth_head_v0 / face_head_v1(自检 0.876/0.974,可直接迁 prod 评估)。
