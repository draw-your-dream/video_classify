# 因子预注册清单(2026-07-28,基于 FAILURE_CATALOG.md)

**范围裁定(2026-07-29,用户决定):只保留 F1-F4 推进;F5/F6/F7 暂缓不用。**
**测量族裁定(2026-07-29):base-224 流水线全量评估显示 F4 有效(15.0%)、F1-F3 未达标;
c_first_last 的 so400m 原版配置远强于 base-224 复刻(26.4% vs 9.7%)——F1-F3 改用 so400m 测量族
(so400m-384 pooler + 方形裁剪,与 crop_feat_original.py 同族)在 H100 重做后再判。**

铁律:因子由失败模式目录先验设计,在此登记后才允许在 prod500 上评估;不许按 27 条上的表现回头挑因子。
评估协议:域内分位 + OR 门;LOO(留一 bad)+ bootstrap 汇报;采纳标准 = ①机制验证通过 ②唯一覆盖 ≥2 条现有轴看不清的 bad ③LOO 下不降低现有门的放行率。
现有基线:dim_fidelity ⊕ c_first_last,27/27 召回,放行 26.4% good(LOO 漏 1/27)。

## 本地可跑(5090,GroundingDINO 每视频 16 帧 + SigLIP2 + OpenCV)

**F1 cap_drift** — 菌盖区域(角色框上 40%)首末帧 SigLIP2 余弦 + 颜色直方图距离。
靶:白点消融/盖形漂移/滑落吞头(64ee, 4c5f, 32da, 5155, 1f65, b729, …≥8 条)。
机制关:语料 skus 还原度 bad vs good 该因子应有分离。

**F2 anchor_min** — min over t of cos(crop_F0, crop_Ft),16 帧。
靶:纯中段漂移(a400 首末均正常的确定性反例);严格强于 first_last。
机制关:在 a400 型样本上应显著低于 first_last 型。

**F3 face_drift** — 脸部区域(角色框中带)F1 同构因子。
靶:凭空长嘴/五官增减(b729, 32da, 5155, 159a, 939c)。

**F4 size_trend** — 角色框高度随时间的趋势/极差,经背景 ORB 单应尺度补偿(排除镜头推拉混淆)。
靶:C 类(78d4, 5155, 5761, e175)。
风险:镜头运动补偿失败时误报;机制关先在有镜头推进的 good 上验证补偿有效。

**F5 nonchar_motion** — 相机运动补偿后,角色框外静物区域的残余光流能量。
靶:I 类(4f95, bd7a, 7a25, 5761)+ B 类滑移。
风险:场景里合法运动主体(人/猫)需检测剔除,否则误报;机制关在含人/宠物的 good 上校准。

## 需短租 GPU(Qwen3-VL-30B,四个定向问题轴,logit(是)−logit(否))

**F6a 悬空** — "这个毛毡玩偶此刻悬在空中/没有站在任何表面上吗?"(B 类)
**F6b 自主运动** — "画面里有没有物体在没有外力的情况下自行移动或变形?"(I 类)
**F6c 静态不可能** — "画面里是否存在物理上不可能的情形:错误的倒影、平面图画有真实深度、不可能维持的平衡?"(D 类——时间信号原理性盲区,唯一出路)
**F6d 凭空出现** — "视频中是否有物体凭空出现或消失?"(F/G 类)
对照:此前笼统问"是否违反物理规律"AUC 0.588;假设 = 定向问题显著更强。

**F7 IP 模板符合度** — prompt 内写死 IP 规范(矮胖短腿、圆头、菌盖带白点戴头顶、毛毡质感),问偏离。
靶:整段偏离型 A(15bf, 8fdc, 159a, 57 61)——首末自比对这类无效,模板写进 prompt = 无域依赖的绝对参照。

## 已知覆盖缺口
- E 类(交互融合,9c51)本轮无专用因子,依赖 F3/F6 兜底;
- 2a8b(H 类)任何因子都不应因它设阈——按 GT 争议处理,若它设阈则单独上报。

## P1 单类 patch 异常(2026-07-30 预注册,语料机制验证先行)

**动机(第一性原理)**:身份属性(拓扑件数/部件位置/质地/配色)不可变,状态属性(姿态/视角/遮挡)自由变。
good 集合定义角色在合法变换下的"轨道",判定 = 是否落在轨道外。缺陷清单映射:
黑点/不合理嘴 = 局部纹理异常;多余肢体/尾巴 = 多余部件(good 库中无近邻);
手脚位置 = 空间关系(需位置条件化,本轮暂不做);身体过长 = 全局几何(patch 盲区,另设比例因子)。
遮挡假警的第一性消歧 = 时间持续性(真缺陷跨帧存在,遮挡只在姿态转换几帧)。

**协议(冻结,先登记后评估)**:
- 提取:16 帧,GroundingDINO 三连 prompt thr 0.25 + 方形 1.25 倍裁剪(crop_feat_original 同款),
  DINOv2-base 518×518 patch tokens(37×37×768),前景 = patch 中心落在检测框内。
- 划分:语料 good 455 以 seed=0 置换,前 300 建库 / 后 155 评估;bad_huanyuandu 464 全评。泄漏隔离。
- 库:bank-good 检出帧前景 patch,每视频至多 1300(md5(rel) 种子确定性子采样)。
- 打分:patch 异常 = 1 − max cos sim(1-NN);帧分 = 前景 top-1%/5% 均值;
  视频分 = 帧分 max / p75 / mean(p75 = 持续性口径)。
- 主指标:bad vs eval-good AUC(fg 口径);bg 口径仅对照。
- **晋级关**:AUC ≥ 0.70 且 fg > bg 口径 → 允许迁移 prod500(库改用 prod good 重建,阈值走分位协议,
  n=27 上不调任何超参);不达标 → 记录后归档,不进门。
- 已知盲区:身体过长/比例类(patch 局部正常)——若语料 bad 中该类占比高,AUC 上限受压,分错误类复盘。

脚本:`extract_patch_feat.py`(特征)/ `patch_bank_eval.py`(建库+打分+AUC)。

### P1 v2 修订(2026-07-30,业界调研后,运行前冻结)

调研依据(要点):
- **AnomalyDINO(WACV'25)**:DINOv2 patch + 1-NN 记忆库为少样本工业检测 SOTA(MVTec 1-shot 96.6 AUROC);
  图级分 = **top-1% patch 距离均值**(tail-value-at-risk),余弦距离——与本协议帧分口径一致,予以保留;
  其 PCA 前景掩码在"对象占画面 ≥50%(close-up)"时失效——我们的裁剪即 close-up,
  故弃 PCA 掩码,用检测框前景掩码(本协议原设计),此为有据取舍。
- **MVTec LOCO / CSAD(BMVC'24)**:缺陷分结构性(纹理/污染)与逻辑性(多余/缺失部件、数量、位置)两类,
  patch-kNN 只覆盖结构性;CSAD 用组件分割+patch 直方图补逻辑性(LOCO 95.3)。
  本轮增设 **s_hist 直方图分支**(bank patch 球面 k-means k=64,帧前景聚类占比 1-NN L1)
  作为逻辑异常的简化探针,靶多余肢体/部件占比失衡。
- **VBench(CVPR'24)**:subject consistency 即用 DINO 特征跨帧余弦——DINO 系对"同类个体差异"敏感,
  适合形象还原判定,支持本轮骨干选择。

修订清单:
1. good 划分改**款分层**(每款按 300/455 比例进库,防某款缺席库导致整款评估误报);seed 确定性不变。
2. 增设 s_hist 逻辑异常分支(同样三口径聚合),与结构分支同表评估。
3. 增加**裁剪对象核验**步骤(verify_crops.py):so400m 零样本分类裁剪(蘑菇角色 vs 4 干扰类),
   win_rate<0.5 进 flag 清单人工复核;并出 款x标签 蒙太奇大图供人工过目——核验不过的视频不参与结论。
4. 晋级关不变:AUC >= 0.70 且 fg > bg;s_hist 单独看,不与结构分支合并调参。

### P1 判决(2026-07-30 深夜,语料 919 机制验证完成)

**未过晋级关,归档不进门。** 全口径 AUC 表(bad 464 vs eval-good 156,款分层划分,泄漏隔离):
最好 s_hist_mean=0.568,结构分支最好 s_top5_fg_mean=0.543;fg 全面>bg(方向对,幅度不足);
剔除跑偏裁剪敏感性三档 0.564–0.571,判决稳健。

**失败机理(核心产出)**:bank_good 分数 P50=0.19 vs eval_good P50=0.42——
开放内容域中每条新视频自带合法新内容(道具/场景/配饰),novelty 噪声地板(0.42)
淹没缺陷信号(bad P50=0.424)。MVTec 范式的封闭场景前提在本域不成立。

**分款分库对照**:池化 0.574(vs 全局库 0.568),款级收紧无效——噪声在款内。
个别款样本小、方差大(花花 top1_fg 0.911@n_good=12,冰晶 hist 0.750,甜品 hist 0.373 反向),不过度解读。
推论:参照系必须收紧到"本视频自身"——**i2v 首帧原图升为头号数据请求**
(自参照 dense 匹配,理论上消灭 novelty 地板)。

**候选因子登记 P2 = 角色在场性(1 − cos_centroid)**:视频前景嵌入到全体中心的余弦,
语料域内单轴 AUC 0.584,专抓角色缺席/被道具顶替;prod500 可用现存裁剪特征直接复算,待预注册评估。

附:v1 零样本核验(so400m 文本塔)在 transformers 5.x 下损坏产生 913/919 假 flag,已作废;
v2 图像侧离心度核验替代,人工蒙太奇复核确认裁剪质量整体良好(检出率 100%,跑偏尾部 ~20 条)。

### 第0步 逐簇 AUC 分解(2026-07-31 预注册,P1 失败后的部件级诊断)

**问题**:缺陷信号是否集中在特定身体区域(脸/肢体),被全身口径的 novelty 地板掩盖?
**方法**(cluster_auc_decomp.py,零新模型,复用 P1 特征与 64 簇):
patch 异常 d 与 P1 同口径;视频×簇分 = 该簇 patch d 的 top-20% 均值(池化全帧);
簇占比 occ 双向轴;逐簇 AUC 仅在该簇出现(>=5 patch)的视频上算,报双侧覆盖;
每簇 12 样本 patch 蒙太奇(行按 AUC 降序)供人工辨认簇语义。
**判读门(先登记)**:存在簇 AUC>=0.65 且覆盖充分(n_bad>=50, n_good>=30)且蒙太奇
显示对应面部/肢体区域 -> 部件线(线A 面部 bank / 线B 肢体计数)立项依据成立;
全簇 <0.60 -> DINOv2+bank 范式对此域缺陷不敏感,部件线改走测量/合成缺陷监督,不再投 bank 类。
**注意**:多重比较(64 簇)下个别簇会偶然高分,故要求覆盖充分+语义可辨认双条件;
本步为诊断定向,不直接产出可上产线的因子。

### 第0步判决(2026-07-31 15:12,新盒复现+逐簇分解完成)

**0 个达标簇,bank 范式在本域正式关闭。**
64 簇外观轴(top-20%)分布:P50=0.502(=抛硬币)、P90=0.552、最高 c16=0.604(覆盖 170/49 不足,
多重比较下不可信);满覆盖(n>=400/140)42 簇中最高 0.587。占比轴最偏 0.613/0.380,同样不可用。
前置复现:新机全链路(下载->提取->核验->评估)与原机千分位一致(s_hist_mean 0.567 vs 0.568,
verify2 cos 中位 0.874 逐字相同),排除"机器/提取漂移"解释——信号就是不在。

**结论链(三级参照系收紧全部失败,机理一致)**:全身 0.568 -> 分款 0.575 -> 逐簇 0.587,
novelty 地板存在于簇内部——同一部位在不同视频里的合法变化(视角/光照/形变/贴身道具)
仍大于缺陷偏移。DINOv2 patch 袋子 + 好样本库这条范式对本域缺陷不敏感,按预注册判读门,
**部件线不再投入 bank 类方法**,转:
  线C 比例轨迹测量(本地即可,零训练)/ 线B 关键点标注+计数 / 线A 合成缺陷监督(免库)。
i2v 首帧原图仍为头号数据请求(自参照才能杀死簇内 novelty)。
蒙太奇 cluster_montage_parts.jpg 留档待人工过目:确认簇确实分出了部位语义
(区分"信号不在"与"分割失败"两种解释,不改变不立项判决)。

## P3 预注册(2026-08-01):SAM3 掩码底座 + 参考图三轴(先登记后评估)

**数据与资源**:tutu-renders-2026-04 参考图 152 张(基础款关键帧 98 + 六饰品款各 9,
combined-1.5x 自带 SAM3 主体掩码);SAM3 权重经 sha256 与官方比对一致(9999e234…)。
前人图片侧管线(Picaa-Image-IP-Similarity-Eval)已消化:检测→SAM 抠像→白底 224 画布
→LPIPS/DreamSim/so400m 对参考集 max→阈值 0.70。其口径成立前提=查询姿态≈参考姿态,
视频域不成立,故弃 LPIPS,继承 SAM3 抠像+白底画布+max-over-refs。

**底座**:每视频 16 均匀帧 → SAM3 文本 prompt(继承前人 "A fluffy mushroom-like creature
with a light-yellow body and a red cap")→ 全实例掩码/框/分;取最高分实例做白底抠像
448 画布存 jpg;记录逐帧:实例数、掩码面积/高宽、伞宽(掩码上部 40% 最大行宽)、
身高(掩码 bbox 高)。蒙太奇人工核验门照旧(抠像质量先过目再进轴)。

**三轴(语料 464 bad vs 156 eval-good,沿用款分层划分,晋级关 AUC>=0.70)**:
- R 参照相似:帧抠像 vs 本款参考抠像池(饰品款 9 张 + 基础款 98 张并集),
  DreamSim 与 so400m-512 图像塔余弦各自 max-over-refs;视频分 = 帧分 p25 / mean(持续不像)。
  前科:前人视频系 fidelity_sku AUC=0.50;翻案三要素=SAM3 掩码/新参考图/DreamSim,不成则永久关闭。
- G 几何:身高÷伞宽逐帧轨迹;静态口径(视频中位 vs 款内 good 分布)+ 漂移口径(末段/首段比)。
  靶身体拉长/比例失衡;视角混淆用持续性压制。
- C 计数:角色实例数轨迹;>=2 持续 K 帧 = 多角色,检出后消失/翻倍类结构异常单独描述统计。
  肢体级计数(arm/leg/tail prompt)为二期,先不跑(成本×4)。
**V 轴(VLM 判官)**:参考图+帧拼图喂新 VLM 按缺陷清单问答——待 R/G/C 出数后单独预注册。
**迁移**:过关轴按分位协议迁 prod500,与现有 dim_fidelity⊕c_first_last 双轴门做 OR 集成,
在 27/27 全召回口径下报告 good 放行率增量。目标:语料与 prod500 双数据集成果。

### P3 判决(2026-08-01 18:10,R/G/C 三轴完成)

**全部未过晋级关(0.70)。** 语料 bad-vs-eval-good:R 轴最好 r_ds_mean(DreamSim 对参照池
平均异离)=0.568,so400m 口径 0.51;G 轴静态 0.53、漂移 0.47(反向);C 轴 0.46-0.47(反向,
实例计数在语料上无信号,SAM3 实例数好坏视频均极稳定为 1)。
prod500 探索性集成(标注为探索,不入正门):基线 26.4% 精确复现;任一新轴并入 OR 门均降低
放行率(13.7%-21.1%),单轴全召回放行 <=10%——新轴抓不住绑定 bad,只多拦 good。
**方法史总结**:至此 0.57 天花板已横跨四个方法族——patch 记忆库(P1)、分款库、逐簇分解、
SAM3 抠像+DreamSim/so400m 参照相似 + 几何 + 计数。唯一强轴 c_first_last 恰是"本视频自参照"。
结论进一步收敛:款级参照与外观度量已穷尽,出路 = 自参照(i2v 原图)/ 判别式(合成缺陷监督)/
语义判官(VLM)。SAM3 底座(抠像/掩码/计数)保留为基础设施,供后续轴复用。

### V 轴预注册(2026-08-01,VLM 判官试点,学长推荐)

**方法**:Qwen3-VL-8B-Instruct(temperature=0),多图输入 = 本款参考图 1 张 + 视频白底抠像
4 帧(f0/f5/f10/f15);中文按缺陷清单结构化问答(脸部黑点/嘴异常/肢体数异常/尾巴/比例拉长/
整体还原 0-10 分),输出 JSON。主轴 = 10 - 整体还原分(越大越可疑);缺陷布尔为辅描述。
**试点**:语料 bad 抽 120 + eval_good 全 156(确定性种子),报 AUC。
**判读门**:主轴 AUC>=0.70 -> 全量+prod500;0.60-0.70 -> 提示词/帧数迭代一轮再判;
<0.60 -> VLM 判官关闭,收敛到 自参照数据请求+合成缺陷监督 两条路。
**前科声明**:前人 VLM 问答轴(fatalflaw/judgment 等)0.42-0.50,但无参考图对照、模型为旧代;
翻案要素 = 参考图对照 + 抠像输入 + 新代 VLM。

### V 轴判决(2026-08-01 19:35,试点完成)

**AUC=0.500,退化输出,按门(<0.60)关闭。** 261/276 解析成功,bad 113 / good 148 全部
fidelity_score=9、全部缺陷布尔为 false——Qwen3-VL-8B 在参考图对照下对本域缺陷粒度不敏感
(reason 字段显示模型在认真看图:能点出四叶草装饰/遮挡/视角,但从不判缺陷)。
可能后续:更大杯 VLM(72B/GPT级)或逐缺陷单独提问属新配置,需另行预注册评估,今晚不做。

### 2026-08-01 战役总结

一晚测掉三个范式:R/G/C(SAM3+新参考图+DreamSim,语料最好 0.568,prod 集成只降不升)、
V(VLM 判官,0.500 退化)。加上 P1/分款/逐簇,**"款级参照+外观度量+语义判官"全路线已穷尽**,
0.57 天花板横跨五个方法族。存活方向(按证据强度排序):
1. **i2v 首帧原图**(自参照;唯一强轴 c_first_last 即自参照的旁证)——头号数据请求
2. **合成缺陷监督**(线A,判别式,免参照;唯一未测范式)
3. **缺陷子类型标注**——解释 0.57 天花板:怀疑「还原度」标签集异质
   (部分 bad 或为运动类/时序类缺陷,静帧不可见),需子标签才能裁决
基础设施沉淀:SAM3 掩码抠像管线(1.2s/条)、152 张参照池嵌入、白底画布标准化——供后续复用。

### V2 + R-self 预注册(2026-08-01 20:10)

**V2(最强 VLM 判官重测,用户指令)**:judge 换 Claude(headless 强推理),输入=本款参考图+4帧抠像,
10 条/批;试点 60 bad + 60 eval_good(取自 V1 试点集,可直接对比);同一 JSON 判词结构。
判读门:主轴 AUC>=0.70 -> 扩全量并 codex/GPT-5.x 交叉验证;0.60-0.70 -> 提示词迭代一轮;
<0.60 -> V 线(含最强模型)整体关闭。
**R-self(语料自参照轴,c_first_last 的语料对应物,首次测量)**:每视频 f0 抠像为自锚,
so400m-512 嵌入,drift_t = 1 - cos(f_t, f0);视频分 = drift 的 max / p75 / 末段均值 / 斜率。
靶中途形变/长出结构/脸崩(自己离自己越来越远)。晋级关 AUC>=0.70;
0.60-0.70 视为 i2v 原图价值的强化证据(自锚是原图的退化版,原图应更强)。

### V2 判决(2026-08-01 20:40,Qwen3-VL-32B)

排序轴 v_main AUC=0.571 < 0.60,按门**关闭 V 线作为排序轴**(8B 0.500 退化,32B 非退化但同撞天花板)。
**预注册外的登记发现**:32B 旗标零误报低召回——other_defect 7/56 bad 触发、good 0/57;tail 1/56。
与标签异质假设定量吻合:若仅 x 比例 bad 静帧可见,理论 AUC≈0.5+x/2;观测 0.571→x≈14%≈旗标率 12%。
六方法族 0.57 天花板的最简一致解释 = 「还原度 bad 中静帧可见缺陷占比 ~15%」。
**裁决实验(预注册)**:盲审页 = VLM 旗标 bad 7 + 随机未旗标 bad 23 + 随机 eval_good 10,乱序匿名,
用户逐条判「静帧能否看出形象问题」;判读:若未旗标 bad 的可见率 < 30%,标签异质假设成立,
语料还原度轴止损,火力全转 prod 域与 i2v 原图;若可见率高,则方法仍有失败责任,线A 继续主攻。

### R-self 判决(2026-08-01 21:00)

**全口径反向(0.41-0.48),关闭。** good 自漂移 > bad——全局嵌入帧间距离量的是动作幅度,
good 动作更丰富;语料 fidelity bad 是静态外观问题,自漂移不可见。
c_first_last 在 prod 有效而其语料对应物反向,再证语料/产线缺陷构成不同
(prod bad 含形象漂移/帧跳变型,语料还原度 bad 不含)。
推论:i2v 原图的价值主张在语料上无法预演,必须直接在 prod 域验证。

### 线A v0 判决(2026-08-01 21:20)

真实 bad 上 p75/max AUC = 0.518/0.527,未过关。**但仪器自检改写解释**:同一个头在
held-out 合成缺陷(eval_good 帧 ± 合成,训练未见)上帧级 AUC=0.876——
black_spots 0.979 / tail 0.996 / elongate 0.969 / smudge 0.945 / extra_limb 0.920(warp 0.521,
合成过轻,v1 待改)。检测器对用户缺陷清单有效,真 bad 上失败 => 语料还原度 bad 中
静帧可见缺陷占比过低(与 V2 零误报 12% 旗标率、0.57 天花板三方互证)。
**标签异质假设升级为主解释,待盲审(39 例已备)最终裁决。**
线A 资产保留:synth_defects.py 七族合成 + synth_head_v0.pt——若盲审证实 prod 域缺陷静帧可见,
此头可直接迁 prod 评估(合成管线与域无关)。

### V3 预注册(2026-08-01 20:50,用户指令:prompt 迭代)

同模型(Qwen3-VL-32B)同输入,三个 prompt 变体,同 120 试点集(降级为 prompt 开发集):
  V3a 先逐帧描述差异再输出判定 JSON(CoT,max_tokens 放宽到 600)
  V3b 基率校准(明示"本批约半数有缺陷")+ 逐帧 0-100 缺陷概率带锚点,视频分=帧max
  V3c 找不同式:列出与参考图所有不同点,逐条 is_defect+confidence,分=缺陷项最大置信
纪律:三变体全报,任何胜出配置须在**新抽不相交 120 条**上确认 AUC>=0.65 才可晋级;
开发集上的最好成绩不作为结论。

### V3 判决(2026-08-01 21:25)

V3a(CoT)0.598 / V3b(基率校准)0.527 / V3c(找不同)0.579,均<0.65 确认线,V 线收档。
机理:V3b 被基率提示带成反向塌缩(好坏全 70-95),V3c 差异全标缺陷置信 95——
prompt 只移动工作点(保守<->激进),判别力钉在 0.57-0.60 = 静帧可见比例的信息论天花板。
开发集最好成绩 0.598 依预注册不作结论。V 系列完整档案:v1(8B) 0.500 / v2(32B) 0.571 /
v3a-c 0.598/0.527/0.579;裁决权移交盲审。

### 盲审结果记档 + 方向修正(2026-08-01 22:10)

盲审(标注者自评不熟悉产品,作弱证据):未旗标bad可见率14/20=70%,VLM旗标确认7/8,
good误判4/9;"静帧不可见"假设被弱证据反向——bad 的偏离(眉毛/外观)人眼可见而机器漏掉。
用户指令:继续提升方法。修正方向:
- E 集成轴(预注册):既有独立机制轴的**无拟合秩平均**(s_hist_mean + r_ds_mean + g_asp_med,
  等权,不调参),AUC 直接报告;任何加权/选择变体都须新样本确认。
- F 脸部高分辨率专线(预注册):SAM3 "face" prompt 定位脸(蒙太奇核验门)→ 从**原生分辨率帧**
  裁脸 → 线A v1 脸尺度训练,合成族增加眉毛(TUTU标准无眉毛,画眉=缺陷)/五官位移/加强warp;
  评估口径同线A(真实bad,AUC>=0.65 关注、>=0.70 晋级)。

### E 集成轴判决(2026-08-01 22:15)

无拟合秩平均:hist+ds 0.574 / +asp 0.570 / +so 0.556,vs 单轴 0.568——
各族轴高度相关(找到的是同一份弱信号),集成无增量,E 线关闭。

### 线F 判决 + 语料还原度轴收档(2026-08-01 23:05)

线F v1(脸部原生分辨率+五族合成):真实 bad AUC p75=0.529;仪器自检 held-out 合成
**0.974**(eyebrows 1.000 / eye_extra 0.984 / mouth_warp 0.977 / spots 0.971 / smudge 0.951)。
覆盖率满(464/464, 156/156)。
**测量链闭合**:对缺陷清单(含盲审点名的眉毛)灵敏度 0.97+ 的检测器,在真实还原度 bad 上
找不到区分信号。结合全身版(自检 0.876/真实 0.518)与两晚全部方法族:
**语料「还原度」标签不是由清单型静态外观缺陷驱动的**(驱动因素:运动/时序伪影、
整体观感、或标注不一致——静帧口径不可恢复)。
校准声明:结论限于"按清单字面合成的缺陷类型";细微比例偏差类未穷尽,但 DreamSim/patch/VLM
对此类也已测毙。**语料还原度轴正式收档**,不再投入。
火力全转:prod 域(27bad 全召回口径,缺陷构成与语料不同已证)+ i2v 首帧原图 + 向学长要
缺陷子类型标注与更多产线标注。沉淀资产:SAM3 全身/脸部提取管线、身体七族+脸部五族合成、
synth_head_v0 / face_head_v1(自检 0.876/0.974,可直接迁 prod 评估)。

---

## 语料放行率战役(2026-08-02)——预注册

**目标口径**:eval_v3(968=405bad/563 good+normal)上 (good+normal) 放行率 @ bad召回;
部署协议:train_v3 侧训练,eval_v3 侧评估;阈值在 eval bad 分数上取分位
(与前人 training/train.py metric_g_n_at_bad_recall 完全同口径,可比)。

**基线(前人 artifacts_v3/predictions_v3.csv 复算,2026-08-02)**:
p_base(meta+boost,可部署):**ev@95=0.1883 / ev@100=0.0107 / AUC=0.7366**。
p_final(14层门级联)ev@95=0.5044、deep 0.7584:前人自证样本内过拟合(RETRAIN.md),不作基线,
但示意 rank 域组合的余量上界。

**E1 复现**:_stack_15expert.pkl + train.py 原配方重训 meta LR(+LGBM boost),
验证 p_base ev@95≈0.188(通过线 ±0.02)。目的:确认重训链路,后续增量实验在此链路上做。

**E2 分位OR门(两个配置,预先冻结,不调参)**:ev15 各专家在 eval 内取百分位秩,susp=max;
(a) QOR-15:全部15专家;(b) QOR-8:仅8个非 per-src 基础专家(先验:per-src 变体与基础版重复信号)。
报 ev@95 / ev@100,对照 E1 meta;阈值口径同基线,另报 LOO 版。
动机:前人自测专家互补(最难20条 base rank 0.083→运动专家 0.399)但用 MAX/meta 联合无提升即放弃;
prod500 上分位OR=26.4% vs MAX=5.9% 已证组合器是瓶颈。此实验裁决"专家联合被错误组合器否定"假设。

**E3 新特征并栈(待盒上全语料提取完成)**:新特征族(SAM3几何/计数、参照相似 r_ds/r_so、
synth_head/face_head 分数)按族 append,同配方重训,报 Δev@95。族清单冻结后评估,不逐特征挑拣。

**裁决准则**:Δev@95 ≥ +0.03 且 ev@100 不降 → 有效增量;否则记录并收档。

**E1 判决(2026-08-02 18:0x)**:复现精确通过。meta+boost:AUC=0.7366 / ev@95=0.1883 /
ev@100=0.0107,与 artifacts predictions_v3.csv p_base 三位小数一致(逐位复现)。
纯 meta(LR):0.7402/0.1936/0.0089。重训链路验证通过,E3 增量实验在此链路上进行。

**E2 判决(2026-08-02 18:0x)**:分位OR门在语料上**失败**。QOR-15:AUC 0.6246 / ev@95 0.0941;
QOR-8:0.6603 / 0.1510。两配置均显著低于 meta 基线 0.188(LOO 召回校验正常 0.948/0.998,
非实现问题)。**判决:「专家联合被错误组合器否定」假设在语料监督口径下被驳回**——405 个 bad
足够监督学习定权,学习加权 > rank-OR;prod 的分位OR优势源于 27-bad 无法监督拟合的小样本处境,
不迁移回语料。二线收档,语料主攻方向唯一:E3 新特征并栈。

**E4 预注册(2026-08-02 18:3x,发车前冻结)**:运动判官 Qwen3-VL-32B-Instruct。
输入=裸视频帧12张等间隔(max边448,不抠像,运动判断需背景参照);中文锚定 prompt,四类运动缺陷
(stiff/jerky/limbs_frozen/no_motion)各0-100 + motion_bad 总分0-100,greedy 解码,断点续跑,
覆盖全 4845。动机:语料 bad 六成运动类,前人 q3vl_motion 弱(~0.54)且用的 8B 级;32B 未测。
评估(冻结):① motion_bad 在 eval_v3 的单轴 AUC 与 ev@95;② 副分 max(四子项) 同报;
③ 5 个分数作为特征族 E4 按 E3-a 机制并栈(同配方),报 Δev@95。判准同 E3(Δ≥0.03 且 ev@100 不降)。
链位:phase2(新特征)完成后自动发车,预计 5-7h 通宵。prompt 一经发车不改;若需改动则作为
新编号实验重新预注册。

**E3 判决(2026-08-02 21:3x)**:全体未过线(判准 Δev@95≥+0.03 且 ev@100 不降)。
覆盖 train/eval 均 100%(4845/4845,22列)。基线 0.1883;E3-a +ALL 0.1812(-0.007)/
+A_geo 0.1794 / +B_ref 0.2007(+0.012,最大单族,但 ev@100 0.0107→0.0071 反降)/
+CD_heads 0.1954(+0.007);E3-b 第16专家单独 AUC 0.553,并栈 0.1829(-0.005)。
**判决:静态形象/几何/参照/合成头特征族在语料监督栈上无有效增量,收档。**
机理与语料保真轴收档判决自洽:语料 bad 六成运动类,静态外观信号本就不是其标签驱动;
前人栈内已有(同样躺平的)保真特征,新特征与之同源冗余。
语料放行率剩余在途:E4 运动判官(32B,通宵,明晨判决)——打的才是语料 bad 的主粮。

**E4 判决(2026-08-02 23:1x)**:**失败**。eval_v3 全覆盖 968/968,解析零失败。
正确口径(midrank AUC,离散分必须):motion_bad **0.5459**,四子分 0.489-0.544,
max/mean 组合 0.543-0.546——全体≈抛硬币。ev@95=ev@100=0.000(48% bad 被打 0 分,
召回阈值只能压到 0)。运动类 bad 子集也无分离。判决:32B 零样本运动判官与 V1/V2/V3
一样死于同一堵墙;**语料上零样本 VLM 判官范式整体收档**(8B/32B、保真/运动、四种 prompt 全试毕)。
train 侧跑批已止损中断(节省 ~3h 盒时),已产出 1950 条留档 data/vlm_motion.jsonl。
**测量事故记录(透明)**:首报 AUC 0.64/子分 0.81 系幼稚 rank AUC 未处理并列值的伪影
(离散分数大量并列在 0,argsort 对并列任意排序且拼接顺序 pos 在前→系统性高估)。
以 scipy rankdata midrank 复核后反转。**教训入规:凡离散/大量并列分数,AUC 一律 midrank;**
E1-E3 复核无此问题(连续分数,结论不受影响)。

**E5/E6 预注册(2026-08-02 23:1x,运行前冻结)**——语料侧不动标签的最后两条合法路。
E5 分源阈值:来源(skus款式/ti2i2v/rlhf)部署时已知。在 train OOF 分数上拟合:
(a) 保守版:每源独立取 95% 召回阈值;(b) 预算版:全局 5% 漏检预算在源间贪心最优分配
(train 上最大化放行)。两版阈值原样搬到 eval,报实际达到的 bad 召回 + 放行率。
若 eval 召回 <0.95 如实报告,不回调。对照:全局阈值 0.1883。
E6 组合器菜单(3 个配置,超参冻结,不再加):
M1 LGBM分类器(leaves31/est300/lr0.05/mcs20)吃 [oof15 ⊕ X320raw] → eval;
M2 同超参只吃 X320;M3 原 LR+boost 配方吃 [oof15 ⊕ z(X320)]。
报三个的 ev@95/ev@100/AUC;多重比较自知(3 配置),判准仍 Δ≥+0.03 且 ev@100 不降。

**E5/E6 判决(2026-08-02 23:2x)**:
E5 分源阈值:**失败/不采信**——train拟合阈值搬到eval召回掉到0.906/0.928(未守住95%约束),
如实弃用。附记:rlhf 源 train+eval 共440条零bad。
E6 组合器:**M1 通过,语料侧首个有效增量**。
LGBM[oof15⊕X320raw](超参预注册冻结):**ev@95=0.2629(基线0.1883,Δ+0.0746)/
ev@100=0.1243(基线0.0107,11.6倍)/ ev@90=0.3730 / ev@80=0.5311 / AUC=0.7437**。
稳健性:5种子零波动;bootstrap 95%CI [0.197,0.330];配对Δ CI [0.009,0.133],P(Δ>0)=0.986。
旁证:M3(LR+boost 加z(X320))0.2611 同量级复现;M2(仅X320)0.2096 也超基线。
机理:前人 meta 只吃15个专家概率=信息瓶颈;尾部bad在专家概率上均庸碌、但在320维原始
特征空间仍可分;GBM 捡回了被专家压缩丢弃的尾部信号。部署合法:单视频打分、特征与
前人栈同源(cache_v3 泄漏修正版)、train_v3→eval_v3 部署协议。
多重比较自认:3配置菜单选最优,但次优复现+配对bootstrap显著,判定成立。
产物:data/s3/m1_lgbm_v1.pkl + predictions_m1_eval.csv。

**E7 预注册(2026-08-03 00:2x,运行前冻结)——训练侧模型选择,单次 eval 出手**:
协议:train_v3 内 5 折分层 CV(seed42,折固定),每候选出全train OOF,选择指标=OOF gn@95
(阈值取 train OOF bad 分位,1621 bad 远稳于 eval 405)。**OOF 最优者唯一获得一次 eval 评估**;
其余候选不看 eval。基线对照=M1 已知 eval 0.2629。
候选菜单(8 个,冻结不加):
C1 M1原样 LGBM(31/300/0.05/20)[oof15⊕X320];C2 同超参 +F22(我方22列);
C3 LGBM(63/600/0.03/20);C4 LGBM(15/300/0.05/40);C5 C1+scale_pos_weight=2;
C6 sklearn HistGBT(31叶/300迭/lr0.05);C7 C1与M3配方的秩平均集成;
C8 C1输入 +F22+源onehot。
判读:若胜者 eval ≥ 0.263 → 新头条;若 < 0.263 → M1 维持,E7 如实记录(不回选)。

**E8 预注册(同时冻结)——机理跨切分复核**:4845 全集按 label×源分层随机重切 80/20 ×3 seeds,
每切分对比 LGBM[X320](GBM吃原始特征)vs LR+boost[z(X320)](线性meta同配方):
检验"GBM组合器优于线性meta"机理是否跨切分稳定。仅作机制旁证,不产生头条数字。

**E7/E8 判决(2026-08-03 00:4x)**:
E7:OOF 胜者 C5(=C1+scale_pos_weight=2,OOF gn@95=0.3103)单发 eval:
**ev@95=0.2647 / ev@100=0.1208 / ev@90=0.3570 / ev@80=0.5240**。
意义:纯训练侧选择(未触 eval)独立复现 26.5%,消解 E6 三配置菜单的选择性质疑;
增量与 M1(0.2629)同水平,无进一步实质提升。部署推荐 C5(协议最干净)。
E8:三个重切分 GBM[X320] 全胜 线性meta[zX320](0.262/0.252/0.284 vs 0.261/0.239/0.234),
机理跨切分稳定。附加发现:线性meta 直接吃 X320 也达 0.23-0.26 >> 15专家概率版 0.188,
证实主效应=解除"15专家概率压缩"信息瓶颈,GBM 在其上再加小幅稳定优势。
产物:data/s3/c5_lgbm_final.pkl + predictions_c5_eval.csv。
**语料头条(协议最干净口径):ev@95 = 26.5%(基线 18.8%),ev@100 = 12.1%(基线 1.1%)。**

**E9/E10 预注册(2026-08-03 11:2x,运行前冻结)**
E9 误拦特征修剪(本地):动机=高分good有5/15由图像嵌入专家指控(把罕见当可疑)。
菜单(冻结):V2=C5输入剔除专家列{siglip,sigb,per_src_siglip};V3=再剔{mp}。
协议同E7:train 5折OOF gn@95 选优(C5基准0.3103);仅当胜者≠C5才单发eval;判准=OOF不降且eval提升。
E10 运动分解特征·本地试点(无掩码v0):动机=用户观察「卡顿/刚体/不连贯一般伴随镜头拉近」
→ 假设:现有运动特征测的是镜头+角色总运动,镜头运动淹没角色僵硬信号。
特征(冻结):相邻帧ORB+RANSAC估全局相似变换(缩放|s-1|=拉近强度),Farneback稠密光流减去
全局模型=残差(角色)运动;逐对特征:zoom、残差能量、运动区面积占比、残差方向/幅度的空间
离散度(刚体度反指标);视频级:各自 mean/max/std + 残差能量时序的帧间跳变(卡顿度)+
zoom×低关节度交互。试点集:本地55条gallery(40尾bad/15高分good,正是钉阈值的前线)+
后台补一批分层语料样本。裁决:若试点显示分离(方向正确且明显),申请盒时跑全4845并栈;
否则记录收档。试点n小,只作方向裁决不报正式数字。

**E9 判决(2026-08-03 11:3x)**:修剪不可行。剔3嵌入专家 OOF 0.2877、再剔mp 0.2863,
均低于 C5 基准 0.3103——嵌入专家真信号大于误拦代价,误拦无减法解,收档转 E10。

**E10 试点判决(2026-08-03 12:0x)**:通过,批准全量。
尾部配对(40尾bad vs 15高分good,现有特征按构造失效区):magcv 0.742 / acf1 0.716(运动尾bad)/
zoom×刚体 0.696 / zoom 0.690——用户「卡顿伴随镜头拉近」观察被定量坐实。
公平配对(30随机运动bad vs 30随机good):res_mean 0.364 / mov_frac 0.333 / coh 0.356
(反向即 |0.64-0.67|)——镜头补偿后运动类bad角色自身运动更少更刚,语义自洽。
两配对方向不一致=交互型信号,适合GBM并栈。判决:批准全 4845 计算(0.44s/条,纯CPU,
盒上~35分钟)→ E7协议并栈测 Δev@95。脚本 scripts/motion_decomp.py 已就绪。

**E10 并栈判决(2026-08-03 12:1x)**:v0(无掩码)未过训练侧门槛。
全 4845 覆盖 100%(16worker 重跑零失败;首跑 48worker ffmpeg 线程耗尽 4710 条 nan,
教训:并行路径必须先冒烟)。C5+12列分解特征 train-OOF gn@95=0.2859 < C5 基准 0.3103,
按协议 eval 未触碰。判决:v0 并栈无增量,收档。
残留假设(允许一次 v1):无掩码整帧残差混入背景视差,测量糙是主嫌;SAM3 重建完成后
可做掩码限定版(角色框内残差)作为独立注册的 v1 实验;若 v1 再败,运动分解方向整体收档。

**E11 预注册(2026-08-03 13:3x,运行前冻结)——视频原生编码器升级专家**:
动机:前人栈内唯一运动原生组件 V-JEPA2(ViT-L 级,专家 0.63)未换代;语料 bad 六成运动类;
"换底模"里唯一未被证伪的一格(编码器非判官,与"单强判官不抬整体"结论不同类)。
做法(冻结):V-JEPA2 ViT-giant(facebook/vjepa2-vitg,64帧口径)对全 4845 抽视频级嵌入;
本地按 E7 协议:嵌入上训 LGBM 头(超参同 E3-b:leaves8/est200/lr0.05/mcs30)5折 OOF 出
第16专家分并入 C5 输入,train-OOF gn@95 ≥ C5 基准 0.3103 才单发 eval;
另报该专家单独 AUC(对照前人 vjepa 专家 0.63)。判准同 E3:Δev@95≥+0.03 且 ev@100 不降。
E10-v1 掩码版(同时冻结):sam3_feat 重建完成后,残差统计限定在角色 bbox 内、背景配准
限定在 bbox 外,其余定义与 v0 相同;同 E7 协议,OOF 过门才动 eval。v1 再败则运动分解方向收档。

**E12 预注册(2026-08-03 14:1x,发车前冻结)——VLM 事实提取器(非判官)**:
动机:VLM 五次排序失败,但旗标信号始终零误报(V2:bad 7/8 确认,good 0/57 误报);
用户证实尾部存在"部分帧眉毛"。设计:不问质量,只问客观事实。
输入=16帧白底抠像(重建中的 sam3_cutouts);Qwen3-VL-32B;prompt 冻结:给官方形象六条
客观规则(无眉/无尾/肢体数/豆眼/嘴),要求逐帧报告确定看到的异常帧号列表,
不确定一律不报(precision-first);JSON 六字段:eyebrows/tail/extra_limb/missing_limb/
eye_anomaly/mouth_anomaly 各为帧号数组。
特征(冻结):每类旗标帧数(0-16)+六类总和,共7列;并栈按 E7 协议(OOF 过门才动 eval);
另报:各旗标在 eval good 上的误报率(检验零误报性质是否保持)与 bad 上的命中率。
运行:主链完成后自动接跑,greedy,断点续跑,约6h。判准同 E3。

**E10 扩展 + E13 预注册(2026-08-03 15:2x,调研后修订,运行前冻结)**
方法论修订(用户指示):单一实现失败不得给方向定论;每环节开变体矩阵,全部在 train 侧
OOF 选优,eval 仍只动一次。收档资格 = 变体矩阵扫尽。
E10 变体族(在 v0/v1 基础上增):
  轨迹系(W2,调研主推):CoTracker3 在 SAM3 框内铺网格点跟踪 → 成对距离变化率(刚体度,
  VMBench-OIS/世界模型果冻效应同款)、轨迹二阶差分(卡顿)、速度自相关、可见性丢失率;
  背景点轨迹单应拟合出镜头运动(VMBench-PAS 同款分离法)。
  先验系:RAFT warping error(EvalCrafter 同款,W3);插帧先验 AMT/RIFE(排明日)。
  经典系:ECC/单应/DIS流/仿射拟合残差(CPU,继续)。
E13 现货打分器族(全空白家族):W1 Q-Align(通用质量,人类MOS训练);明日 DOVER、
VideoScore2/VideoReward(AIGC偏好训练)。每模型输出1-4列分数。
统一裁决:全部新列(E10全家+E13+E11+E12)进一次性的 train-OOF 大选优(菜单=按族组合,
冻结后执行),唯一胜者单发 eval。任何族的收档判决只在大选优之后作出。

**训练侧记分牌(2026-08-03 19:3x,eval 零接触)**:四个新测量族全量到手
(V1掩码分解 97.7% 完好 / W2轨迹 96.8% / W3RAFT 99% / W1打分器 100%)。
单特征 train AUC 全弱(最强 |0.5±0.07|)。并栈 OOF:C5+V1=0.2983 / +W2=0.3076 /
+W3=0.2983 / +W1=0.3001 / 四族全并=0.2952 / 精选12列=0.2881 / 精选4列=0.3045,
**全部低于 C5 基准 0.3103**。与 E10-v0 同机理:弱信号列稀释尾部排序。
Q-Align 附记:美学分方向正确(运动类bad ~0.60 等效),质量分反向(生成的僵硬静止视频
画面反而"干净")——有趣但并栈无增量。
待完成后进大选优:E12 旗标(在跑)、vjepa2-g(E12 后重试,路径bug已修)、DOVER/VideoScore2(明晨)。

**第二波攻击预注册(2026-08-03 19:5x,用户指示追求本质超越)**
E14 尾部定向目标(本地,冻结菜单):C5 配方之上改训练目标——
  (a) 难例加权:bad 样本权重 ∝ (1 - OOF分),幂 γ∈{1,2} 两档;
  (b) 两阶段级联:C5 全局分 + 模糊带(OOF分位20-60%)专家,final=带内用专家分重排;
  (c) LGBM lambdarank(pairwise)目标替代 BCE。
  全部 train-OOF gn@95 选优,唯一胜者才动 eval。
E15 置信学习(本地):cleanlab 思路,OOF 概率与标签强烈矛盾的 train 样本
  (bad 而 OOF<q10(good分布) 或反向)降权 {0.3, 0} 两档重训;eval 标签零改动。
E16 视频编码器微调(明日 GPU):VideoMAE-v2/vjepa2-L 在 train_v3 端到端微调
  (VMBench-CAS 配方),OOF 由 2 折微调近似;判准同 E3。
E17 密采样脸扫(明日 GPU):48 帧脸部提取 + face_head 逐帧 max;
  E12 旗标同口径整合为「否决门」:拦截 = 分数≥T 或 旗标命中;
  否决门的 T 重定标在 train 侧完成,eval 单发。

**E14/E15 判决(2026-08-03 20:1x,均未触 eval)**:
E14 尾部定向目标全灭:难例加权 γ=1/2 → 0.2952/0.2633(追噪声,越加权越差);
级联带内重排 0.3103(尾部在带外,无效);lambdarank 0.1051(全局排序目标崩)。
E15 置信学习:非嵌套 w=0 曾报 0.3551(+4.5),**嵌套验证归零(0.3059)——选择循环性伪影**。
教训入规:任何"用模型分数选样本再重训"的方案,OOF 报数必须嵌套;非嵌套数字一律不采信。
E15 的副结论有价值:模型强烈不同意的 10% train bad 标签,剔除后并不能改善诚实 OOF——
"标签噪声"如果存在,不是这种"模型可分辨"的形态;进一步支持人工盲重标(E5路线)而非算法自清洗。
第二波剩余:E12(在跑)、E16 编码器微调、E17 密采样脸扫+旗标否决门、E11重试、DOVER/VideoScore2。

**E12 判决 + 规则线开张(2026-08-03 22:4x,全部 train 侧)**:
E12 全量 4845 完成(解析 98%)。当特征并栈:OOF 0.2863 < 0.3103,**不过门**——与四层机理
预言一致(旗标当特征无用)。当否决规则(train 侧):**eyebrows 旗标 = 头号规则**:
152 bad 命中 / 31 标good(精度 83%,且 31 条"误杀"待人工审——可能含真眉毛的标签噪声);
mouth_anomaly 过度报警(280 good)待剔;tail/eye/missing 弱;extra_limb 零触发。
运动/计数规则(误杀预算 6/2222):刚体式移动 14bad / 静止 11 / 剧变 11 / 美学低 11 /
撕裂 8 / 推拉 7 / 卡顿 3;消失/多实例/识别崩在极端阈值下零命中。
**审核 gallery 已交付用户**(Downloads/TUTU规则审核gallery:5 外观规则帧拼图 + 6 运动规则视频),
用户逐规则批准后才冻结联合、单发 eval(两种口径:纯否决拦截率@错杀、混合制放行率增益)。

**E11 判决(2026-08-03 23:2x)**:vjepa2-giant 专家单独 train-OOF AUC 0.6505(前人 vjepa-L ~0.63,
单体小幅进步;eval 单轴 0.6437 按预注册报告项披露);并栈 OOF 0.2992 < 0.3103 **不过门**,
eval 栈未触碰。判决:冻结编码器形态收档;编码器线仅剩 E16 微调(通宵在跑,VideoMAE-base
二折无泄漏 train 分 + full 模型 eval 分,明晨门检)。

**E16 判决(2026-08-03 23:5x)**:VideoMAE-base 端到端微调(2 epoch,16帧,二折无泄漏):
单独 train AUC 0.5585(弱于冻结 vjepa2-g 专家 0.65),并栈 OOF 0.2988 < 0.3103 不过门,
eval 未触碰。校准:此为最小配方,更大底座/更多轮次未试;但结合前人监督判官三连阴性与
本周全部结果,模型侧对语料栈的收益证据已收敛于零。**模型侧战役收官**:C5(26.5%)为
本阶段语料侧最终成绩。明日:DOVER/VideoScore2 补完打分器家族(完整性)、规则线待用户
审核后出否决联合判决、大选优走完形式、汇报页定稿。

**规则线 v2 修订(2026-08-04 上午,依据用户逐规则人工审核)**:
用户判定:眉毛相对准(窄眼误判+2例截取失败);尾巴须区分身长vs穿戴;眼睛高光/遮挡误判;
缺肢剔除(多为遮挡);嘴巴现役不行但真崩坏存在须重设计;静止/变大误判源于镜头距离,
须bbox归一化与镜头缩放补偿;刚体误挑成"紧致不动",改为"动而不变形"联合判据;美学加辅助分。
v2 动作(冻结):E12-v2 精修prompt(四条排除规则)对 v1 旗标子集重问;bbox_h 归一化列;
R6' v_rel=v_mean/bbox_h384;R7' 刚体=v_rel>中位 且 pd_cv 低;R-变大' area_range 对
(1+zoom_tr_mean)^15 补偿;W4 pyiqa 四指标美学辅助;缺肢规则移出池。
v2 规则重定标仍在 train 侧;更新版小样本 gallery 交用户二次确认后冻结联合。

**E12-v3 预注册(2026-08-04 11:5x,用户指示:抠像升级方案A)**:
诊断:白底抠像抹掉持物/穿戴语境(望远镜→眼睛异常误报;背包带→附属结构误报),
且 SAM3 按置信度选实例会锁错玩偶(用户在gallery发现2例)。
v3 提取(冻结):SAM3 出全部候选实例;选择=对152张官方参考图嵌入的最大相似度
(认人代替认置信度)+ 帧间IoU连续性;QC=参考相似度过低标记抠像失败并排除出旗标规则;
VLM 输入改为 bbox 外扩25% 的原画面裁剪(保留背景/持物/穿戴)。
E12-v3 = v2 精修prompt + v3 裁剪,先对旗标候选(~640)重问;三代对比(v1/v2/v3)分离
prompt修正与输入修正的贡献。运动/几何管线不动。

**E18 预注册(2026-08-04 13:5x,用户指示训练线再调优)**:系统性训练侧搜索。
空间(冻结):LGBM 超参(leaves 7-63/est 100-800/lr 0.02-0.1/mcs 5-60/spw 1-3/
feature_fraction 0.5-1/bagging 0.5-1)× 输入变体{oof15⊕X320, 仅X320, oof15⊕X320⊕分源onehot}
× 分源独立模型开关;Optuna ~150 trial,目标=5折 train-OOF gn@95(折固定 seed42)。
产出:①最优单配置;②OOF 前5配置的秩平均集成。二者中 OOF 高者为胜者,单发 eval。
诚实条款:大规模选择会给 OOF 带来乐观偏差,eval 判决以 Δev@95≥+0.02 为过线
(低于常规+0.03,因本实验无新特征、纯调参,预期增益本就小);若 eval < 0.2647(C5)
则维持 C5,记录搜索无效。

**E18 判决(2026-08-04 14:1x)**:**通过——语料头条更新**。
150-trial Optuna 训练侧搜索(目标 OOF gn@95,eval 单发):最优单配置(input=full,
OOF 0.3218)eval:**ev@95=0.2913(C5 0.2647,Δ+0.0266>+0.02 过线;基线 0.1883 的 +55% 相对)/
ev@90=0.4334(C5 0.3570)/ AUC=0.7593(历史最高)**。
诚实披露:ev@100=0.0409 低于 C5 的 0.1208——E18 与 C5 构成工作点帕累托:
95%/90% 口径部署 E18,100% 全召回口径部署 C5。复现:TPE seed=7 确定性,重跑存档冠军中。

**规则线 eval 首判(2026-08-04 14:3x,用户指令开刃;冻结版=v1旗标+运动v2训练阈值)**:
纯否决:并集拦 73/405 bad(18.0%),错杀 91/563 good(16.2%),精度44.5%。
单规则:眉毛 31bad/7good(81%,与train侧83%一致,唯一达标规则);嘴部 32/65 差;运动规则量小。
混合门:放行 23.1% < E18 单独 29.1%(净负6点)——与train侧尾部分析预言完全一致
(规则抓的多为模型已能拦的bad,却误杀本会放行的good)。
**判决:规则线对语料放行率无增益(结构性:尾部覆盖9/82封顶);其价值重定位为
可解释缺陷报告+跨域(prod)迁移候选。**E12-v3(裁剪修正版)出料后仅更新精度记录,
不再改变语料侧结论;v3 的主战场转 prod500。

**第三波预注册(2026-08-04 14:5x,用户指示:新信号/新专家/结构性,不要调参)**
E20 过拟合保险:2 个新随机切分(seed 101/202,80/20按label×源分层)重演 E18 全流程
(60-trial 训练侧搜索,eval 每切分单发);输入含 X320-only 变体(专家分跨切分有轻微
口径混用,如实标注)。判读:E18配方在新切分上仍胜基线配方=过拟合疑虑清除。
E21 码流专家:ffmpeg 导出 H.264 运动向量+宏块+逐帧大小;特征=MV幅值/散度统计、
I块占比序列(尖峰=跳变)、帧大小变异、P/B帧MV熵。全语料,CPU。
E22 首帧漂移专家:DINOv2 对每帧×首帧的相似度序列(全帧与角色框内两路);
特征=漂移斜率/终值、最大突变、突变时刻、曲线下面积。i2v 首帧=生成条件,前人未做。
E23 深度物理专家:DepthAnything-v2-small 逐帧深度;特征=角色框内深度中位轨迹的
平滑性、角色-支撑面深度差稳定性、深度突变帧数。
E24 分缺陷多头(结构性):train 上 6 个一对好 LGBM 头(僵硬/卡顿/四肢不动/还原度/
物理/其他 vs good,reason 标签仅训练侧),OOF 出 6 列,并入冠军输入重训。
判准:各新专家先单独过训练门(OOF≥0.3218 基准之上为增益,记录单专家AUC);
过门者合并单发 eval。

**E24 判决(2026-08-04 15:2x)**:**不过训练门**。6 头组内 AUC 尚可(僵硬0.840/还原0.822/
少动0.809/物理0.732/卡顿0.705/画面0.642)——按缺陷分治能学到类内信号;但并入冠军后
train-OOF gn@95=0.3160 < 0.3218:头的信息冠军已从原始特征自行学到,无增量。
E24b(6头max秩融合,"任一头高分即拒")=0.2629 更差——模型侧再次复现规则线结论:
单指标一票否决范式在本语料天然亏 ev@95。按协议不动 eval。

**E20 判决(2026-08-04 15:2x)**:**过拟合疑虑清除**。
切分101:搜索冠军 ev@95=0.3333 | C5配方 0.3032 | 前人式meta 0.3422
切分202:搜索冠军 ev@95=0.3475 | C5配方 0.3369 | 前人式meta 0.3245
①搜索式流程在两个全新切分上均胜 C5 固定配方(+3.0/+1.1pp)——E18 的增益是流程性的,
非本切分彩票;②前人式 meta(仅15专家分)1胜1负,iid 切分下与冠军相当——原始特征的
优势主要兑现在正式切分的跨源尾部上,与 E6 信息瓶颈论一致;③新切分绝对值(0.33-0.35)
高于正式 eval(0.2913),说明正式切分确实更难(源分布移位),无泄漏迹象。
诚实标注:两个新切分把原eval行混入训练侧,其15专家分是原train上训练的产物,
三臂同条件相对比较成立,绝对值仅供参考。

**难bad增补gallery用户判读(2026-08-04 16:0x,35条主线盲区bad,用户人工审核)**
运动类确认:#1/3/4/5 刚体+卡顿;#6-9 少动不自然;#11-14 刚体僵硬少动;#33 僵硬刚体。
结构/外观新发现:#16 长出5指(用户指示可新增规则类);#17 无脚;#19 多余肢体(身后拉物);
#34 长出眉毛——**VLM v1/v3 双漏检的眉毛案例**;#35 突然换装。
物理类确认(直指E23/E22/E21新信号):#20 大小变化违透视;#21 背景完全不动;#22 悬空无支撑
(E23 support-gap 的正中靶心);#23 穿模;#24/25 玻璃投射违物理;#31 透视不符+无支撑。
画面组定义(用户):背景运动混乱=明显背景变化/镜头切换/突然新增物体(=E21 I帧尖峰/E22 全局
maxjump 的目标)。
待办:新特征到位后用这35条做逐条命中核验;v4规则候选=手指计数/明显无脚/多余肢体/突然换装。

**E21/E22/E23 判决(2026-08-04 16:4x)**:**三族均不过训练门**。
E21 码流(12列):冠军+并入 OOF=0.3174;E22 首帧漂移(12列,覆盖99%):0.3138;
E23 深度物理(10列,覆盖99%):0.3090;三族全并 0.3183——均低于冠军基准 0.3218,
单列 midrank AUC 全在 0.43-0.55(无单列强信号)。按协议 eval 零接触。
但用户35条盲区bad逐条核验显示特征在个案上正中靶心:
#20 透视违规→cd_med_cv P96;#22 悬空→gap_mean P95;#26/27/29 场景突变→g_maxjump P82-90;
#04 卡顿→mv_mag_cv P87。个案命中≠聚合增益:把这些特征当 P95/P98 阈值规则测精度,
train/eval 全在 40-46%≈基础率(train bad率42%)——尾部个案强、整体无分离力,
与"尾部难bad是异质小样本"的结构判断一致。gap_mean q98 eval 89% 为跨切分不一致的
孤立点(train 42%),不采信。
**第三波总结:E20 保险通过;E21-E24 四路新信号/新结构全部诚实证伪。语料侧战线收官:
头条 E18 ev@95=29.1%(基线18.8%),累计 20+ 路预注册证伪构成完整排除地图。**

**E12-v4 预注册(2026-08-04 16:4x,用户第二轮审核意见)**:眉毛线加固——错杀主因=
窄眼/眯成一条线被误认作眉毛;v4 要求同帧同时看到眼睛+其上方分离线条才算眉毛,
只见一条线且下方无眼睛一律按眯眼处理。尾巴/眼睛/嘴巴三线适当放宽(较明显即可报,
不再要求绝对确定),排除规则(穿戴物/高光/遮挡/说话开合)保留。输入不变(crops_v3),
v1 旗标候选全量重问,v3 的 46 条 error 行一并补问。判准:眉毛精度不降召回回升,
或错杀(good旗标)显著下降;三放宽线各自 bad/good 计数供用户三审。

**E25 预注册(2026-08-04 16:5x,用户指示:把我们标准文档的判断方法整理后直喂大模型,多试)**
材料=《三档分级标准》判定方法:五维标准+两步流程(缺陷排查一票否决→good/normal区分)
+13标签一句话判据。模型=Qwen3-VL-32B(在箱)。输入=全帧(非抠像,标准要看背景/物理/画面)。
试点=train 抽 150(bad75/good75,按源分层,seed5),四变体:
P1 标准整理版全文+三档输出+bad_prob;P2 检查单版(13标签逐个0/1+三档);
P3 两步流程CoT版(先逐维排查后判);P4 = P1 prompt + 视频原生模式/32帧密采样(对照16帧图列)。
判准:midrank AUC(bad vs 其余)对比历史 zero-shot 最好 0.60 与特征线 0.759;
三档放行精度另报。胜者若 AUC>0.65 才考虑全语料+栈门,否则记录证伪。
试点为 train 侧 prompt 开发集,eval 零接触。
**E12-v4 预注册(同时段,用户二审意见)**:眉毛=同帧须见眼+上方分离线条,只见一条线
按眯眼处理不报;尾巴/眼睛/嘴巴放宽为"较明显即可报";排除规则保留;输入crops_v3不变,
v1候选全量重问含v3错行。判准:眉毛错杀降、三线召回升,数字供用户三审。E25 后串行。

**E25 修订(2026-08-04 17:0x,发车前调研后冻结)**:据 GenArena(pairwise>>pointwise,
Qwen3-VL pairwise 可胜专职RM)、Artifact-Bench(二元74%>>细粒度9.8%;CoT有害;稀疏采样
是运动瑕疵漏检主因)、Q-Bench-Video(LMM 在 AIGC 失真维度离人最远)修订变体:
P1 标准整理版+三档(pointwise对照);P2 检查单二元分解(13标签逐个yes/no);
P3 锚定成对(候选vs同源最自信good锚,双向问序消位置偏差,胜率作分)【替换原CoT变体】;
P4 = P1 + 原生视频模式(24帧+sample_fps元数据)。预期基线:打破 zero-shot 历史最好 0.60
即有意义;0.65 过线才考虑全语料;特征线 0.759 不设为目标。

**E25 判决(2026-08-04 17:4x)**:**四变体全败,证伪收档(未过0.65线,不上全语料)**。
P1 标准整理+三档 AUC=0.600(三档塌两档,0个normal;放行精度60%);
P2 检查单二元 0.500(1950问仅7个"是",全零塌缩);P3 锚定成对 0.547(位置偏差
0.87/0.61,严格双确认51%=基础率,复现Artifact-Bench PVRC≈随机);
P4 原生视频模式24帧+fps元数据 0.493(123/150全喊bad,反向塌缩)。
结论:①0.600=zero-shot判官天花板第6次确认;②四变体四种塌法=无稳定工作点,
非prompt工程问题;③与Q-Bench-Video(AIGC失真为LMM最弱维)/Artifact-Bench
(Gemini 3.1 Pro 47.5 vs 人87.7)一致。VLM唯一站得住用法仍是E12事实提取器。

**E12-v4 判决(2026-08-04 18:2x,待用户三审定谳)**:眉毛 v4 train 79/18(81%) eval 14/5(74%),
较 v3 少报 49 条(43bad/6good 撤旗,v4 零新增)。注意:视频级标签≠旗标正确性——被撤的
43 个bad可能本就是"其他原因bad+窄眼误旗",撤对撤错须人工判,已出撤旗抽样20条 gallery。
放宽线:嘴部 6/9(40%)+eval 2/2(50%),召回如预期回升,精度仍弱,19条全量供审;
尾巴/眼睛放宽后仍各1-2条(穿戴/高光排除主导,放宽无效)。外观并集 v4 86/27(76%)
低于 v3 82%:若三审证实撤旗多为正确(窄眼),则 v4 旗标正确率实际更高,精度表失真。

**两把钥匙规则预注册(2026-08-04 19:1x,用户拍板发车)**:结构=特征预筛(第一把)+VLM事实
确认(第二把),针对E21-23"个案强/聚合无分离力"与E25"全库判官塌缩"的互补设计。
R1悬空:e23 gap_mean≥锚#22值(有限分位P95,名单178,bad率45%)+VLM问"是否持续悬浮无支撑"
(排除跳跃/被举/坐趴/遮挡)。R2场景突变:e22 g_maxjump≥最低锚值(P82,名单323截320,bad率39%)
+VLM问"背景物体是否瞬间出现消失替换/镜头切换"(排除平移拉近/主体动作/光影)。
各配60条盲样对照(VLM不知情)测空转率。判准:名单内确认者的bad率须显著高于名单bad率
与盲样确认率;规则命中出gallery交用户审。R3背景全静:特征钥匙校准失败(#21背景残差P83,
角色像素污染背景估计),挂起待重设计,如实记录。eval零接触。
**E12-v5 预注册(同时段)**:v4基础上新增 fingers(手指分明,排除模糊遮挡)/no_feet(全身
可见站立且腿末端无脚,排除坐趴遮挡裁切)/extra_limb(可见肢体>4或来历不明第三肢);
**嘴部回退v3严格版(用户指令,v4放宽作废)**;眉毛沿用v4窄眼规则待三审。
范围=半量省钱:good全量2256+冠军OOF最低25%尾部bad 406=2662条(用户选项);
新视频需先补crops_v3提取。判准:新类命中bad/good计数+gallery供用户审。

**W5 预注册(2026-08-04 19:3x,视频输入+质量校准兼备的现成模型,补 W1-W4 缺口)**:
W5a DOVER(fragment采样保原生帧率时间密度,UGC-VQA监督,输出 technical/aesthetic 两分);
W5b VideoScore2(Qwen2.5-VL-7B 底座,VideoFeedback2 人工标注上 SFT+RL,三维度:
visual quality / t2v alignment / physical consistency——我们无生成prompt,alignment 维
喂占位文本,如实标注该维不可信,主用 visual+physical)。范围:train 全量打分,
分数并栈过训练门(0.3218);单列 midrank AUC 另报。主要风险:UGC→二次元AIGC域移(W5a),
prompt缺失(W5b)。排程:今晚链(两把钥匙+crops+v5)完成后接跑;下载先行(网络不占GPU)。

**两把钥匙首判(2026-08-04 19:5x)**:
R1 悬空:**第二把钥匙成立**——名单确认36/178(20%),盲样1/59(2%空转);锚#22正确确认。
确认者bad率53%(名单45%),36条含17条标注good→ либо VLM误读 либо 标注漏网,
已出 gallery(TUTU悬空规则gallery,K编号)交用户定谳。
R2 场景突变:**第二把钥匙失效**——确认仅7/323(2%)≈盲样(0/60),用户锚#26/27/29全漏:
相邻帧突变发生在抽帧间隙,VLM无帧对帧比对机制。E22特征(第一把)+VLM(第二把)对称双残,
此规则收档;备选路线=显式帧差检测(传统CV,非VLM)另行评估。

**两把钥匙v2预注册(2026-08-04 20:0x,用户审核悬空gallery后修正)**:用户判读:部分悬空
误报实为"有支撑物/手臂扒拉物体支撑";场景突变判据应限定"凭空从无到有",镜头运动带入
新内容不算。v2修正:R1悬空排除条款加攀附/扒抓/挂靠/倚靠;R2场景突变限定同位置凭空
出现消失,排除镜头带入,帧数12→24(密采样给突变落进帧对的机会),R2借此复活重试一次
(锚#26/27/29须命中,再漏即终判收档)。同名单+同盲样重跑,排在v5链后(GPU互斥)。

**E12-v5 判决(2026-08-04 22:4x,半量集=good全量2256+尾部难bad406,有效2432/2662)**:
新类别首战失利:fingers 4命中全为标注good且至少1例确认为"人手托举玩偶"误读(prompt未排除
画面人手);no_feet/extra_limb 零命中且用户锚#17/#19全漏(前提过严或448px全身裁剪手脚过小);
锚#16五指也漏。修正方向:v6=排除人手+手脚局部特写裁剪,另行排程。
**重大正收获:眉毛全量重跑价值获硬证据**——锚#34(v1/v3双漏)抓回(帧0-11),且在v1候选池外
新命中16条(bad8/good8):v1全语料初筛存在假阴性,直接全量问可回收。嘴部回退版2命中(均good)。
gallery=TUTUv5新类gallery(N编号)待用户审。

**v5 fingers 四命中逐条复核(2026-08-04 22:5x,用户指出后重看,以用户终验为准)**:
6182=TUTU本体机甲手套金色五指分明——规则抓对特征(标注good或因款式/轻微→normal侧,
与标准"手指略分明判normal"一致);15177=人手托举误报;20086=画中男孩手误报;
17159=企鹅玩偶脚爪误报(模型note自认"非角色本体"仍旗标,措辞失效实证)。
v6手指条款:只判蘑菇TUTU本体肢体末端;画面中人类的手、其他玩偶/动物爪一律排除;
规则报事实(帧号),档位留给标注。先前"全是人手误报"的判读有误,已更正。

**两把钥匙 v2 判决(2026-08-04 23:1x)**:
R2 场景突变 v2:**半复活(精度型)**——确认14/323,bad 9/14(64%,基线39%),盲样1/60(2%);
三锚中一(#27 note具体正确:第8帧键盘上方突现格纹布料),#26/#29仍漏。按预注册严判召回
不达标;按用户"宁漏勿错杀"范式记为精度型报告器,14命中入gallery待审。
R1 悬空 v2:**重排除塌零**——178条确认0,锚#22 note明写"全程悬空无任何支撑"而hit=0:
非感知失败,是"只有当…才算+长排除清单"把输出先验压死(同E25-P2全零塌缩)。
提示词工程铁律再证:排除条款要加在原结构上,一次一句;重写判定框架=换塌缩方向。
v3(v1原文+仅攀附一句)已发车重跑。

**悬空 v3 判决(2026-08-04 23:3x)**:v1原文+单句攀附排除=恢复正常。
确认31/178(bad 16/31=52%),盲样2/59(3%);锚#22回归命中。vs v1:撤5(3bad/2good)、零新增
——攀附排除仅影响边缘案例,v1确认里15条标注good仍在(用户复审定谳:VLM误读 vs 标注漏网)。
两把钥匙终版:悬空v3(31条)+场景突变v2(14条,精度型)=45条入终审gallery。
提示词版本学:v1可用→v2重写框架塌零→v3单句增量修复,"排除条款只做增量"铁律成立。

**E27 判决(2026-08-05 16:2x,用户问询触发,盒上复测)**:最新一代旗标(眉毛v4∪v5
98条bad80/good18、悬空确认31条16/15、突变确认14条9/5)作3列特征并入冠军:
train-OOF 0.3090 < 基准 0.3218(-1.3点),**不过门**。82条尾部难bad旗标仅命中5——
与08-03第一代旗标结论(0.2863)一致:检测器抓的是模型已能拦的易bad,38条good旗标
反而注入标签噪声。"检测器并入总分"路线终判关闭;两线分工维持(总分管放行率,
检测器管可解释报告/标注复核/生产迁移)。
**W5 发车(2026-08-05 16:4x)**:W5a DOVER(批量版,冒烟实分)→ W5b VideoScore2
(2fps帧+风格占位prompt+CoT正则解析,冒烟3/3/3、3/1/3,~4.5s/条,全量约6h)。
判准照预注册:单列midrank AUC + 并栈过训练门0.3218。

**E12-v6 探针判决(2026-08-06,4目标+3对照,眉毛三配置×尾巴二配置)**:
输入=crops_v3(448) 时宽窄两版问法全灭(对照全对)→ 病根=分辨率:脸部仅占裁剪小块,眉毛~十余像素。
**放大二探(上部62%×2放大)+宽措辞:H086/H089/H101 三条眉毛目标全部命中**(H086全帧/H089帧13/H101帧11),
7060正对照保持;代价=负对照7241回归误报(帧1-8)。三探(放大+严格窄眼排除):7241误报不消,
反把H086/H089压回漏检——严格条款在放大视图下无净收益。
结论:①召回杠杆=放大,非措辞;②v6定型=放大+宽措辞(3/3回收,1/2负对照FP),
按精度优先两线定位,FP由人工审核吸收;③全量v6=裁剪补提+放大重问(~5h)待发车。
尾巴H099:粉色锚定/泛化问法/全帧输入三配置均未见,人工看16帧亦未定位——需用户指认出现时段。

**三路特征化判决(2026-08-06 17:5x)**:
①僵硬(最重要正结果):刚体15列全量落地(nan2%)。**物理方向全部验证**:僵硬bad的
刚体拟合残差更低(rr_mean反向AUC 0.725)、象限分歧更低(0.715)、静止段高频更低——
"刚体假设成立度"是真信号。仅刚体15列组内AUC 0.765;E24式基线0.847→+刚体 0.854(+0.7pt);
**报告器 top60 精度 88%**(历史最强可解释僵硬打分器)。冠军并栈 0.3067<0.3218 不过门
(第4次复现:组内强≠总分增,尾部异质)。判决:僵硬报告器成立,出名单交用户审。
②眉毛蒸馏:帧AUC对教师0.994;七锚留出:**抓到教师漏检的H101(0.996)**+H086边缘(0.63),
漏H089与教师易案7060,负对照基本干净——学生+放大可超教师的概念验证成立,但98旗标视频
的标签量不足以稳定,升级需更多标签(v6全量VLM或人工)。
③尾巴粉色特征:锚5822完美触发(persist1.0/hue172.5真粉);hue145-179收紧后全库候选63条
(含锚);bad率40%≈基础率但KPI是尾巴召回非bad率——定位=第一把钥匙,63条待第二把确认。

**E30-eval 预注册(2026-08-06 20:2x,用户指令门外单发)**:四路线均未过训练门,用户要求
看eval;只发 OOF 最高的 C 路线(僵硬头OOF单列并入冠军,train 0.3196 vs 基准0.3218)。
单发一次,对照 E18 冠军 eval 0.2913;如实入账,不论涨跌。

**E30-C eval 判决(2026-08-06 20:5x)**:ev@95=0.3126(+2.1) ev@100=0.1226(+8.2,超C5的0.1208,
帕累托消失) ev@90=0.4369 AUC=0.7606——全口径优于E18。诚实标注:train-OOF略低于基准仍在
eval上涨,+2.1在±5噪声带内;此为第9次eval接触,为防"选择在eval上",启动E20式双新切分保险
(train侧,冠军配方 vs 冠军+僵硬头列)——两切分均胜才转正为部署候选。

**眉毛两把钥匙首判(2026-08-06 21:1x)**:CNN蒸馏预筛(btop3排序top400)→VLM放大确认:
178条判有眉毛,bad精度83%(147/178);**历代未旗标新命中27条(22bad,含6条eval bad)**——
全量重扫的召回价值坐实。缺口如实入账:CNN过饱和(bmax≥0.99达393条),top400截断把
三个锚(5719/6565/6228)都挤出名单;剩余790候选未确认。蒸馏v2待办:用本轮178+222确认
标签重训、校准输出分布;或直接VLM扫完剩余790(约1.8h)。

**E31 判决(2026-08-06 21:3x):E30-C 保险未通过,不转正**。
切分101 冠军0.3422 > +僵硬头0.3316;切分202 冠军0.3191 < +僵硬头0.3422;@100双双打平。
一胜一负=僵硬列效应为切分噪声级。E30-C 在正式eval的全口径上涨(31.3/12.3/43.7)记为
"观察性占优、保险未证因果"——16955(僵硬bad钉ev@100)被抬升的机理故事真实,但不构成
可复制的方法增益。**汇报口径维持 E18 29.1%**;E30-C 存档为部署可选项(期望不劣于E18,
实测eval更好,但不得宣称+2.1为方法贡献)。僵硬信号战役至此完整收档:
报告器成立(88%@top60),总分增益证伪(E27/E30四路/E31保险三重验证)。

**E32 判决(2026-08-07 12:0x,零新特征五路,全部 train-OOF)**:**全灭,基准 0.3218 未被超越**。
A 种子集成:x3 0.3276 / x5 0.3169 / x10 0.3178——x3的+0.6是集成规模上的非单调噪声,
不构成方法(真增益应随规模单调稳定);B 异构集成最好 0.3178(LR单独0.2713/中容量0.3103);
C 分源分位归一 0.189(灾难性:分源归一破坏跨源可比,把易放行源的配额让给难源);
D 冠军⊕C5 最好 0.3178;E 剪枝 top100 0.3227(+0.09,噪声级)。
结论:**组合器层的免费午餐已吃尽**——集成/融合/剪枝/归一四类通用手法在本任务上均无稳定增益,
因为瓶颈不是方差或噪声特征,而是尾部信息缺失。放行率的下一次真实提升只可能来自
新标注(学习曲线+3点/翻倍未饱和)或新数据源,不可能来自现有信号的再加工。

**W5 判决(2026-08-07 15:0x):现成视频质量模型族全灭,方法族穷尽完成**。
DOVER(fragment采样保原生帧率,UGC-VQA监督)三列单列 AUC 0.519/0.523/0.523;
VideoScore2(Qwen2.5-VL-7B,VideoFeedback2人工标注SFT+RL)三维 0.493/0.470/0.490
——**全部≈随机,甚至 align 维反向**。并栈:+DOVER 0.3223(+0.0005噪声级)、
+VS2 0.3191、+全6列 0.3125,均未过门。覆盖率 100%,非提取失败。
机理:两者度量的"质量"是压缩伪影/清晰度/通用美学(DOVER)与通用AIGC失真(VS2),
与本项目"IP还原+活物感"缺陷正交;VS2 的 align 维因无生成prompt(喂占位描述)不可信,
但 visual/phys 两维同样无信号,排除prompt缺失的解释。
**"直接评价视频优劣的现成方法"至此全部试完:Q-Align/pyiqa四件套/DOVER/VideoScore2 + 
zero-shot VLM 六轮 + 视频编码器四路,无一例外。**

**E34/E35 判决(2026-08-07 15:0x-17:0x):端到端直判创历史最好单体,但并栈不过门**。
E34 深监督直判(crops_v3 8帧→timm主干→时序池化→多任务头,5折OOF):
e34a effb0+6辅助头 AUC 0.6650 / gn@95 0.1782(端到端历史最好,前值E16 VideoMAE 0.559);
e34b convnext_tiny AUC 0.5066(未学动,lr不适配);**e34c 无辅助头消融 AUC 0.6646——
与e34a持平,证明多任务辅助监督零贡献**,0.665来自端到端学习本身。
E35 并栈:冠军+e34a 0.3174、+e34c 0.3147、基准0.3218,**均不过门**。
E36a VLM QLoRA(Qwen3-VL-8B)首跑 AUC 0.5470——**实现缺陷,作废**:labels未mask提示段,
对数千图像token算loss(收敛在7.0,正常SFT应<1),判别信号被淹没。已修(只对assistant回答
算loss),小样本验证AUC 0.49→0.63,全量重跑为 E36b。
**历代微调底座对照(查证)**:v2=Qwen3.5-9B(有泄漏);v3=Qwen2.5-VL-7B-Instruct,8帧@短边192,
单字"好/坏",1ep,r=16 → 诚实AUC 0.656,现役15专家之一;生产域=Qwen2.5-VL-7B+Qwen3-VL-8B
5折;E16=VideoMAE-base 0.559。E36 的三处新意:新底座、336px角色中心裁剪(v3仅192px整帧)、
带缺陷类型的文本目标(但E34消融提示第三点预期应打折)。

**E36b/E37 判决(2026-08-07 23:2x):VLM微调修正版回到历史线,并栈仍不过门,微调路线收档**。
E36b(Qwen3-VL-8B QLoRA,loss掩码修正,336px角色裁剪,2折):loss 7.0→0.27(修复确证),
fold0 AUC 0.6768 / 全量 **0.6491**、gn@95 0.0638。对照:E34a端到端CNN 0.6650、
之前系统v3(Qwen2.5-VL-7B,192px)0.656、E16 VideoMAE 0.559 —— **修正后与历史最好持平,未超越**;
折间波动 0.677→0.649 显示 3877 样本对 8B 仍偏紧。
E37 并栈:冠军+e36b 0.3152、+e36b+e34a 0.3147、秩融合最好 0.3191(λ=0.1),**均不过基准 0.3218**。
值得记录:e36b 与 e34a 秩相关仅 0.184(两种深度学习范式学到的东西几乎不相关),
但即便如此互补也没换来增益——再次印证尾部信息不在像素表征里。
**32B 决策:不上**。8B 修正版仅与历史持平且方差大,瓶颈是样本量非容量;32B 参数×4 而数据不变,
预期只会加大方差。微调路线(三代底座×四配置,0.559-0.677)如实收档。

**E38/E39/E40 预注册(2026-08-08 00:0x,用户指令:纯方法层面、不引入新数据)**:
从第一性原理定位——放行率仅由"冠军低分区里的尾部bad vs good"决定,故三路直攻该区:
E38 条件式尾部专家:冠军分低分区(q∈{0.3,0.4,0.5})内单独训专家重排序,两段拼接;
  嵌套无泄漏(折内4折定分区)。动机:该区分布与全局不同,全局模型的容量被高分区占用。
E39 目标函数对齐:PyTorch MLP,①soft-quantile 代理直接优化 P(good<T_5%bad);
  ②尾部局部 pairwise(仅低分区 bad×good 对,区别于崩掉的全局 lambdarank 0.105);
  ③二者与 BCE 的混合。同折 OOF。
E40 结构约束:按 5 折符号一致的方向给 top-K(20/50/100)特征加 monotone_constraints;
  另加 interaction_constraints(15专家分|320原始各成组)。动机:小样本下正确先验>调参。
判准统一:train-OOF gn@95 > 0.3218 且优势稳定才谈 eval。

**E38/E39/E40 判决(2026-08-08 05:3x)**:
E38 条件式尾部专家全灭(q∈{0.3,0.4,0.5}×2容量,最好0.3041<0.3218)——低分区单独建模
反而丢失全局信息,两段拼接排序破坏了跨区可比性。
E39 目标函数改造全灭(soft-quantile 0.2238 / 尾部pairwise 0.2451 / 混合最好0.2788;
同架构纯BCE 0.2544)——**交叉熵不是瓶颈**;直接优化指标使梯度只由极少数尾部样本决定,
过拟合噪声。注:MLP 架构本身弱于 LGBM(BCE 0.2544 vs 0.3218),故 E39 内部比较有效、
跨架构比较不适用。
E40 结构约束:单调约束 top20/50/100 = 0.3107/0.3059/0.3085 全灭(方向稳定特征仅162/335,
强加方向伤害大);**交互约束(15专家分|320原始各成组)= 0.3271 > 基准 0.3218,唯一过线**。
**E41 三关验证**:①多种子胜4/5、均值+0.0083;②分组变体——随机等分对照 0.3191(低于基准)、
三分组 0.3267、专家分独立+全交互 0.3218(=基准,实现正确性自检通过);③双切分保险 1胜1负。
证据弱正但未达"两切分均胜"标准。**E42 高统计力终判发车**(20折种子配对+95%CI+Wilcoxon,
6新切分保险,随机分组同步20种子对照);判准发车前冻结:CI下界>0 且 6切分≥4胜 → 申请eval单发。

**E42/E43 判决(2026-08-08 22:4x):交互约束——训练侧统计显著,eval 零增益,不改口径**。
E42 高统计力检验(判准发车前冻结,两项均达成):
①20折种子配对 均值 **+0.0078±0.0024**,95%CI **[+0.0032,+0.0125]**(下界>0),胜14/20,
  Wilcoxon **p=0.0036**;**随机分组对照均值 -0.0004、CI含0、p=0.809**——增益确属
  "禁止专家分×原始特征跨组交互"这一具体先验,非"加约束即好"的通用效应;
②6个全新切分 胜4/6、均值+0.0059。
**E43 eval 单发(第10次eval接触)**:ev@95=**0.2913**(与冠军分毫不差)、ev@100=0.0178
(低于冠军0.0409)、ev@90=0.3925(低于0.4334)、AUC=0.7577(低于0.7593)。
**判决:train 侧 +0.78pt 的统计显著增益在 eval 上完全没有兑现,其余口径反而略降。
维持 E18 冠军为唯一部署与汇报口径(29.1%)。**
方法论收获(重要):train-OOF 上通过 20 折配对、95%CI、随机对照三重检验的效应,
在 968 条 eval 上仍可能归零——**+0.8pt 量级的方法改进已低于本任务的可验证分辨率**。
这为"组合器层免费午餐吃尽"提供了最强的一次证据:不是没找到增益,是找到了统计显著的增益
但它小到无法在真实评估集上体现。至此纯方法层面(E32五路+E38/E39/E40/E41/E42六路)全部收档。

---

**杠杆结构测算(2026-08-09,发车前的定位分析,非实验)**:
首次把"放行率还能从哪来"算成可执行的账。eval 阈值 T=0.2259 由**分数最低的 20 条 bad**顶住:
| 修好最低 k 条 bad | T | 放行率 |
|---|---|---|
| 0(现状) | 0.2259 | 29.13% |
| 5 | 0.2545 | 33.75% |
| 10 | 0.2708 | 37.48% |
| 20 | 0.2985 | 43.34% |
**每修好一条 tail bad ≈ +0.9 个百分点**——这是全任务唯一的杠杆。解释了 E32/E38–E43 为何全零:
它们改的是全局排序,一条都没碰这 20 条。这 20 条的 E18 分数 0.039~0.224,**低于 good 的中位
0.304**,即模型认为它们比一半可放行样本还好。

**规则线对放行率零贡献(重要的方向纠正)**:眉毛 v6 在 eval 命中 35 条、精度 83%(29bad/5normal/1good),
但这 29 条 bad 的模型分数中位 **0.685**(阈值仅 0.226)——**模型本来就全部拦下了**;
∩tail20 = **0**,∩tail60 = **1**;误拦代价也是 0(命中的 6 条 good/normal 同样在阈值上方)。
与"僵硬 reporter top60 ∩ tail bad = 0"同构。**结论:规则线是可解释性资产(给人可核对的理由),
不是放行率资产,继续投入不会动 29.1%。**

**三档标签的信息量(E44/E45 的立论依据)**:splits 一直存着三档标注(train bad1621/good1302/
normal954;eval 405/325/238),历来被压成二分类 0/1,丢弃了 954 条 normal 的档位信息。
冠军在从未见过 normal 标签的情况下自发学出 **good 0.304 < normal 0.378 < bad 0.555** 的序
——序真实存在。但需预先声明头寸上限:normal 放行率 19.7% vs good 36.0%,即便 normal 全部
追平 good 也**封顶 +6.9 个百分点**且不可达;故 E44/E45 的真正指望不在"多分一类",而在
**序数框架下"bad判成good"(跨两档)重于"bad判成normal"(跨一档)的损失不对称,把梯度压向尾部**。
与 E39(失败的尾部损失)的本质区别:E39 人为定义尾部,本路线用的是标注者给出的真实边界。

**E44 预注册(2026-08-09,组合器层,不吃 GPU)**:同折(seed42)、同冠军参数,基准 0.3218。
变体 A 3类softmax(score=P(bad) / P(bad)+λP(normal), λ∈{0.25,0.5,0.75} / 期望档位);
B Frank-Hall 序数分解(M1=P(y≥normal), M2=P(y=bad),w∈{0.2,0.35,0.5} 及秩平均);
C 不对称样本权重(normal 权重 β∈{0.5,0.75,1.5,2.0});D 序数回归 y∈{0,1,2};
E 两阶段秩融合;F 3类模型的 P(normal) 作为新特征(嵌套无泄漏)。
判准(发车前冻结):train-OOF gn@95 > 0.3218,且须在多种子复核中稳定,才谈 eval。

**E45 预注册(2026-08-09,VLM 微调,承接 E36 的 0.649/fold0 0.677)**:两处改动——
①目标格式:答案首 token 由二元「是/否」改为三档「好/中/差」,读 3 个 logit 归一化,
score = P(差)+λP(中)。动机:E36 用单个「是」token 概率打分,分辨率天生粗且饱和。
②底座升级:8B → **Qwen3-VL-32B-Instruct-bnb-4bit**(官方预量化 20.4GB;实测 30B-A3B 无预量化
需 62.2GB,下载成本高 3 倍而激活仅 3B,故改选 32B 密集)。
**主指标改为 tail-AUC(E34/E36 教训的直接应用)**:AUC 不是标准——E34 的 0.665、E36 的 0.649
都不差,入栈后无一过 0.3218。定义:取 E18 train-OOF 分数最低的 20% bad(tailB,即顶住阈值者),
对全部 good+normal(REL)算 AUC。E18 在此指标上按构造接近 0;VLM 若 tail-AUC > 0.55,
说明它看到了 E18 看不到的东西。**判准发车前冻结:fold0 的 tail-AUC ≤ 0.55 即判该配置对放行率
无用,立即停、不付满折的钱;> 0.55 才跑满折 OOF 并入栈验 gn@95 > 0.3218。**
2×2 消融(隔离"模型变大" vs "目标变好"):8B×{binary,tri} × 32B×{binary,tri},
其中 8B+binary 用 E36 历史值 0.677(fold0)。附加变体:16 帧、MLP-LoRA、λ 扫描。

**E44 判决(2026-08-09):三档序数监督在组合器层全灭,18 变体无一过基准**。
基准复现精确 0.3218(三档对齐断言通过:good1302/normal954/bad1621)。完整排名:
| 变体 | gn@95 | | 变体 | gn@95 |
|---|---|---|---|---|
| **基准 二分类** | **0.3218** | | B Frank-Hall w=0.35 | 0.2957 |
| C normal权重 β=0.75 | 0.3205 | | B4 Frank-Hall 秩平均 | 0.2939 |
| C normal权重 β=2.0 | 0.3183 | | E 两阶段秩平均 | 0.2939 |
| F +P(normal)列 | 0.3174 | | B Frank-Hall w=0.5 | 0.2912 |
| C normal权重 β=0.5 | 0.3165 | | A2 λ=0.5 | 0.2810 |
| B Frank-Hall w=0.2 | 0.3138 | | A3 期望档位 | 0.2810 |
| C normal权重 β=1.5 | 0.3059 | | A2 λ=0.75 | 0.2784 |
| A1 3类softmax P(bad) | 0.3041 | | D 序数回归 | 0.2748 |
| A2 λ=0.25 | 0.3036 | | B0 M1单独 P(y≥normal) | 0.2686 |

**机理**:三档标签没有引入新的**输入信息**,只改了**目标**;而 E39 已证明"交叉熵不是瓶颈"。
15 专家 OOF ⊕ 320 特征这个表征已把可提取信息榨干,在固定表征上改目标只是重新加权。
**方向性观察(有信息的负结果)**:λ 单调递减(0.3041→0.3036→0.2810→0.2784),
即把 normal 往 bad 侧推严格有害——**normal 在决策边界上更靠近 good**,
这反过来证明历史上"normal 并入 good 的二分类"是正确选择,而非信息浪费。
**此判决不外推到 E45**:组合器层的三档是"固定表征上换目标",VLM 微调的三档是
**训练信号作用于表征学习本身**,两者不同构,E45 仍按原判准执行。

**E45 批次1/2 判决(2026-08-09):三档目标、尾部加权均未过尾部门**。
fold0(2 折切分的第一折,覆盖 1429/1938,受 crops_v3 覆盖率限制,与 E36 同条件):
| 配置 | 整体 AUC | **tail-AUC** | 判决 |
|---|---|---|---|
| 8B + 三档 | 0.6529 | **0.5242** | ✘ |
| 8B + 二分(对照) | 0.6464 | **0.5155** | ✘ |
| 8B + 三档 + 尾部加权 α=2.0 | 0.6530 | **0.5289** | ✘ |
| 判准(发车前冻结) | — | > 0.55 | |

**判读要点(避免读数陷阱)**:tail-AUC 的有效参照系是**随机 0.5**,不是 E18 自身的 0.4011
——后者是构造性偏低(tailB 本就是被 E18 判最低的那批 bad),任何独立模型都会高于它。
故 0.52~0.53 的含义是:**微调后的 8B VLM 对"顶住阈值的 bad"与"可放行样本"几乎无区分力**。
三条正交路径——换目标格式(三档 vs 二分)、改训练分布(尾部加权)——**全部钉在 0.52~0.53**,
与"尾部 bad 对像素派生表征不可见"这一反复复现的结构性发现完全一致。
**效应量记录**:三档确实优于二分(tail-AUC +0.87pt / AUC +0.65pt),方向为正但量级远不足以撬动
放行率;尾部加权 α=2.0 相对无加权仅 +0.47pt,可视为噪声。
**32B 未得结论**:unsloth 预量化仓库与 bitsandbytes 0.50.0 不兼容
(`fix_4bit_weight_quant_state_from_module` 断言失败),已改下官方 Qwen/Qwen3-VL-32B-Instruct
(66.7GB)+ 运行时 nf4,容量维度仍待验证。
**副产品发现(进批次3)**:我们的纯二分变体 0.6464 低于 E36 历史 fold0 0.677,
唯一差别是 E36 目标文本附带缺陷类型词(多任务)。**该文本信号可能值约 +3pt AUC**,
此前从未单独测过,已加 `--multitask` 单独验证。

**工程侧记录(提速 27 倍,后续实验直接复用)**:初始实测 7.8 秒/样本,fold0 需 5.6 小时。
诊断:GPU 利用率 **0%** 而 load average 8.3——瓶颈是单进程的 cv2 解码 + Qwen image processor,
GPU 在空等(机器实为 112 核 / 1TB 内存)。三处改动:①8B 从 nf4 改 bf16(仅 16GB,H100 放得下)
7.8→4.2s;②DataLoader 24 进程预取使 CPU 预处理与 GPU 计算重叠 4.2→**0.29s**;
③两实验并行使 GPU 达 **100%**(显存 69/80GB)。fold0 从 5.6 小时压到约 15 分钟。

**E46 预注册(2026-08-09,用户提议:专门在最差的那些样本上单独训练)**。
与两个已失败的近邻实验的**本质区别**必须写清,否则是重复劳动:
 · E38(组合器层条件式尾部专家,已灭 0.3041):在**固定表征**上按分区重排序;
 · E45 `--hard-weight α=2.0`(已灭,tail-AUC 0.5289):全样本训练、尾部**温和加权**(≤3 倍);
 · **E46:让表征学习本身只看尾部**——训练集直接砍成 tailB + 选定负样本,是该思路的最极端版本。
设计:训练集 = tailB(E18 train-OOF 判最低的 20% bad)∩ 训练折 + 负样本,三个变体——
 `hard_neg`:取 E18 分数**最高**的等量 REL(即阈值附近、模型最想拦的可放行样本)。
   这一条最对准目标:放行率的高低,本就取决于"阈值附近 tail bad 与 REL 谁在上谁在下";
 `balanced`:随机等量 REL(对照,用于区分"难负样本"与"只是样本变少"的效应);
 `all_rel`:全部 REL(保留类别不平衡)。
**无泄漏**:tailB 由 5 折 OOF 定义;训练只取训练折内样本,验证仍在完整验证折上算指标。
**已知风险(发车前声明)**:fold0 训练样本从 1938 砍到约 320,VLM 微调易过拟合;
故 epochs 提到 3 并同时看整体 AUC——若整体 AUC 大幅塌陷而 tail-AUC 虚高,判为过拟合而非真信号。
判准沿用冻结标准:tail-AUC > 0.55。

**E48 判决(2026-08-09):E47-C 的 +5.61pt 判为伪影,不进 eval——且推翻脚本自身的"通过"**。
四对照结果:③多种子 真列 0.3716 vs 基准 0.3155(**Δ +5.61pt,5/5 全正**);
①置换检验 0.3082;②纯噪声列 0.2982;**④覆盖子集内 真 0.2649 vs 打乱 0.2643**
(打乱分布 0.2609~0.2744,**上沿反超真值**)。
脚本按 `真 > 打乱均值` 判"通过"(差 0.0006),**该判准写得过松,人工推翻**:在无缺失可利用的
子集里,32B 真实分数与随机打乱表现等同,与其满折 tail-AUC 0.5316(近乎随机)自洽。
同栏还暴露:无该列基准 0.2461 → 加**打乱列**即 0.2643,**加一列纯噪声就 +1.8pt**,
说明该组合器对新增列本身存在方差敏感性,这是此后一切"加专家列"实验都必须扣除的基线。
**教训:自动判准必须写成"高于对照分布上沿",不能写成"高于对照均值"。**

**根因发现(比上述实验更重要)——训练集有 26.5% 的缺口,且几乎全是 bad**:
| | 覆盖 crops_v3 | 缺失 | 缺失内 bad 率 |
|---|---|---|---|
| train_v3 | 2849 | **1028(26.5%)** | **97.18%**(999 bad/15 good/14 normal) |
| eval_v3 | 954 | 14(1.4%) | 50% |
`load_frames()` 读不到 crops 即整条跳过,故 **E36/E45/E46 全部九个 VLM 配置的实际训练集只有
2849 条、bad 率仅 21.83%**(真实 41.8%);**缺掉的 999 条占 train 全部 bad 的 61.6%**,
且正是最极端的那批(连角色都框不出)。
**这提供了比"缺陷在像素中不可分离"更简单的解释**:九个配置齐刷刷卡在 tail-AUC 0.5155~0.5324,
可能仅仅because 它们训练时没见过六成 bad。**此前把该天花板归因于表征极限的结论,就此挂起。**
注:train 缺 26.5% 而 eval 仅缺 1.4%,不像"bad 视频检测失败"的自然结果(eval 有 405 bad
却几乎不缺),更像 train 侧提取未做完。

**E49 预注册(2026-08-09)**:`load_frames` 增加 `--frame-source`——
`crops`(历史行为)/`auto`(无裁剪则回退原始 mp4 整帧,覆盖恢复 3877)/`video`(一律整帧)。
1042 条缺失视频已由 presigned URL 全部补回(1042/1042)。
两个独立假设一并检验:①**覆盖率**:补回 999 条极端 bad 是否突破 0.52 带;
②**背景信息**:crops 是角色裁剪,而"背景运动混乱/物理规律/帧跳变/突然新增物体"等缺陷
在裁剪后不可见,`video` 模式测此项。
判准沿用冻结标准 tail-AUC > 0.55;并须与 `crops` 模式同条件对照,以隔离两个假设。
工程注意:并行 cv2 已按事故记录设 `OPENCV_FFMPEG_CAPTURE_OPTIONS=threads;1` +
`cv2.setNumThreads(1)` + workers ≤16,且发车前先冒烟。

**E49 判决(2026-08-09):数据缺口假说被证伪,且方向相反——补回 999 条 bad 后更差**。
同代码、同超参、同折划分、**同指标池**(有 crops 的 2849 条,tailB=124/REL=2227,
E18 自身 tail-AUC 0.2719),唯一变量是训练数据来源:
| 组 | 训练集 | 整体 AUC | tail-AUC |
|---|---|---|---|
| video | **3877 条整帧**(999 条极端 bad 全部回归) | 0.6206 | **0.4730** |
| crops(对照) | 2849 条 SAM3 裁剪(历史行为) | 0.6383 | **0.5201** |

**故"九个配置卡在 0.52 是因为训练时没见过六成 bad"这一解释被推翻**,此前挂起的
"缺陷在采样帧像素中不可分离"重新成立。
**但须声明该对照混淆了两个因素**:video 模式同时改变了①训练集覆盖率(+1028)与
②角色的有效分辨率——整帧 resize 到 336px 后角色仅占画面一小块,而 crops 把 336px 全给角色。
更差的结果里,分辨率损失与覆盖率收益的净额为负,**无法单独归因**。
若要彻底分离,需跑"整帧 + 高分辨率(672px)"一组;鉴于 tail-AUC 已达 0.4730(低于随机),
判定该路线投入产出比过低,不再追加。
**统计力限制(如实标注)**:fold0 覆盖后 n_tail 仅 **59**,0.4730 与 0.5201 之差(4.7pt)
在此样本量下不具统计显著性,两组的正确读法是"同样都没过门",而非"video 显著更差"。

**VLM 微调路线收档(2026-08-09)**:累计 **11 个配置**——目标格式(二分/三档)、训练分布
(温和加权/极端截断×2)、文本信号(多任务缺陷词)、空间分辨率(448px)、时序分辨率(16 帧)、
容量(32B,单折假阳性 0.5727 → 满折 0.5316)、数据来源(整帧/裁剪)——
**tail-AUC 全部落在 0.4730~0.5324**,判准 0.55 无一穿过。
未解疑点(留档,不影响本结论):train 缺 crops 26.5% 而 eval 仅 1.4%,该不对称尚无解释。

**E50–E53 冻结预注册（2026-08-10 13:10:33 +08:00，S0 前）**：完整不可变协议正文为
`data/prod500/E50_PREREG_DRAFT.md`，505 行、36,376 bytes，SHA-256=
`23d6cb356cd271e02fee1b8d9ead9e660af1f6dbabd493433f9f3e4931545c35`。35% 为主目标、50% 为
stretch；唯一业务口径仍是 95% bad 召回下 GN 严格 `<T` 放行。S0 builder SHA-256=
`4e69b4bc46ed3c0dc5ec756a28f0be072f160dc9d035ee67f977f60a0f88a65f`，必须一次性原子建立
25% prospective shadow、5-fold discovery outer folds 与每个 outer 内冻结的 4-fold inner split；
S0 后不得重分。配对 E18 train-only nested-meta builder SHA-256=
`a04900603f2e35dd6ccff18a67ce5fdf88734e945f3cddf3b5bfe0a96f0b53b6`；strict rank/frontier
harness SHA-256=`66dc519841074c9f578cb186d4732788a8e2d0fb5dfafd700e8b74ea1a1f7119`。

本冻结轮次唯一可运行候选为 **E51**：V-JEPA 2.1 Base-384、64 帧、长边双端点视图、冻结骨干与
固定 local dense head。extractor SHA-256=`b639dcc04dc60fae26c3a5725f3eebc46121b62e270c89f7e260ebb4b96bc37e`，
官方 checkpoint SHA-256=`848a77c33cc9e6649ed2119c9bea1e2c569bcdab9539ff3e7c02ccc2959ddf4d`，
G0=`PASS`；head SHA-256=`b10a0dcf0bc15ffb15e568f81d2587998284c9497424e3a21af40b3b7dd423ee`，
synthetic self-test=`PASS`。E50 官方 TRAJAN G0 因 425 秒仍停在全 corpus 字节 hash、尚未用 GPU 而
记 `DEFERRED_BY_COST`（不是负结果、不是 G0 失败）；E53 同步 `DEFERRED_BY_COST`；E52=
`BLOCKED_BY_DATA`；full 3DSPA=`BLOCKED_BY_UPSTREAM`。冻结时尚未创建 S0，未运行任何
E50–E53 标签指标或 shadow，未读取 eval 标签/特征/分数/指标，未运行最终 eval。后续任何协议、
代码、权重或候选变更必须新立编号并重新建立 S0，不得修改本条或正文后续解释当前结果。

**E51 判决（2026-08-10）：有效负结果，35/50 discovery 门均失败，不进 shadow/eval。**
S0 一次性冻结 3,877 条 train 为 discovery=2,908、prospective shadow=969；E51 特征
2,908/2,908 完成、missing/error=0。固定 5×4 nested head 的 candidate bundle SHA-256=
`0ff6f27e6e5ec38037f9412ae5f8984e4dd92ff2d84b78c7b68001a396d505a2`；与 paired E18
严格合并后的 discovery bundle SHA-256=
`4f6fc393a1032ab9374e4b6ebf94b9ce1c04680b388dd072a051da909541c53d`；完整 5,000 次
bootstrap + 200 次 matched-null 裁判 SHA-256=
`dbfe47d9939c3134dbf1e1a2d51b05bf9af118ef07ef61f5dcfe3fac4d8bce2f`。

同协议 E18 `gn@95=0.3026005`（512/1692 GN 放行），E51 固定公式后
`gn@95=0.2978723`（504/1692），配对 Δ=`-0.0047281`，95%CI
`[-0.0301418,+0.0206856]`；5 折增益符号为 `0,+,+,-,-`。Frontier-AUC35 虽有点值
`0.6089`，但 95%CI=`[0.4123,0.7358]` 且未超过单家族 max-null 99% 分位；
Frontier-AUC50=`0.4750`。整体 midrank AUC 也从 `0.751064` 微降到 `0.750630`。
故不是“差一点达到35”，而是主业务点值和整体排序都没有正增量；按冻结停止条件不得消费
prospective shadow。shadow 标签、eval 标签/特征/分数/指标和最终 eval 均未打开。

---

**E50 预注册(2026-08-11,用户指令:全力提升放行率,集中僵硬/刚体/物理,或全量跑初版眉毛检测)**。
**定位分析(先于一切实验)**:eval tail20 缺陷构成 = 运动族11条(僵硬6/卡顿6/少动3,有重叠)
+ 还原族8条(大小变化3:16713/17148/16396;还原度4含5822尾巴;衣服一致性1)+ 物理1。
**用户审计的 H086/H089/H099/H101 = 5719/6565/5822/6228,全部落在 tail20 内**,
其中三条眉毛可判、一条尾巴粉色物体。
**历史缺口确认**:brow_confirm 仅覆盖 850 候选中 top-400(CNN 饱和 393 条 bmax≥0.99 所致),
四条目标全部未进过 VLM 确认——6228 btop3=0.79 被截断挤出,5719(0.444)/5822(0.430)低于
0.5 阈值,6565(0.042)被 CNN 漏掉。**是覆盖问题,非检测能力问题。**

**E50 = 规则否决门(veto gate)混合制**,三条轴:
 A 眉毛:补齐 brow_confirm 全部候选(btop3≥0.4,无截断)+ **eval 全量 968 条 VLM 扫描**
   (脱离 CNN 依赖,规则=「头部放大后 VLM 确认眉毛」,prompt 冻结自 v6 已验证版本,81-84% 精度);
 B 大小变化:新客观检测器,crops_v3 逐帧 bbox 尺寸轨迹(锚样本 16713/17148/16396);
 C 尾巴粉色:tail_pink.csv 特征已有(锚 5822 persist=1.0),规则化 + VLM 确认。
**混合制记账法(冻结)**:否决集 V 并入后,阈值 T 重新按 95% 总召回定
(被 V 拦截的 bad 计入召回),放行率 = 未被否决且 p<T 的 good+normal 占比。
每否决一条本可放行的 good ≈ -0.18pt,每拦下一条 tail bad ≈ +0.9pt。
**纪律**:检测器参数/prompt 全部冻结自 train 侧既有开发;先在 train-OOF 上算混合制增益
(须 >0.3218),过门才做 eval 单发(第 11 次接触)。
**须如实声明的偏差**:轴的选择受用户对 eval 难例审计的启发(选择性偏差风险);
但检测器本身未针对 eval 标签调参,且 eval 全量扫描为 label-blind。
**僵硬/刚体(运动族 11 条,最大头寸)**:另行探索——光流场渲染成图喂 VLM(设计过未执行)。

**E50 中期判决(2026-08-11)**:
**A 眉毛全量:核心战果**。1283 条零解析错误。用户审计的三条眉毛目标 **5719/6565/6228 全部
VLM 确认**(6565 为 CNN 整个漏掉、VLM 放大后抓到;5822 正确未判眉毛)。**∩tail20 = 3(原为 0)**。
eval 侧命中 30(17 bad/9 good/4 normal);train 侧联合否决集(历史178∪新80)= 193 条
(158 bad/16 good/19 normal),train 混合制 0.3227(+0.09pt,过 0.3218 门但如实声明:
train 尾部仅 3 条眉毛 bad,train 门在此轴上检验力弱;eval 应用的真正依据是规则确定性
+ 用户已亲验三条目标)。
**C 尾巴:失败(按冻结版)**。77 候选 VLM 确认 0 条,锚 5822 亦未确认(note:"未观察到粉色凸起物")。
判读:粉色块仅占裁剪面积 0.7%,下半身 70%×2 放大仍不足。**不迭代救**:train 侧无锚可开发
(唯一已知正例 5822 是 eval 样本),迭代 prompt = 对 n=1 的 eval 调参。轴关闭,留档。
**D 运动族:失败(按冻结判准)**。盲测 exp 90.0% vs ctl 58.6%(四肢无变化旗标率),
差 31.4pt < 40pt 判准;完全冻结率 50.0% vs 41.4% 更差。关键失败模式:**对照组假阳性过高**
——好视频里 TUTU 四肢确实常不动(毛绒玩偶的活物感多来自弹跳/摇摆而非四肢摆动),
「四肢是否动」不是区分僵硬 bad 的有效事实。运动族 VLM 事实路线收档。
**B 大小变化 v1:失败**。200 候选确认 21(train 精度 47%),**三条锚全漏**,VLM note 自信
("比例保持稳定")。判读:渐变尺寸变化摊到 12 帧每帧仅 ~2%,跨帧比较是 VLM 盲区。
**B2 预注册(v1 失败的呈现方式修正,非阈值调参)**:首末帧并排两图直接对比,
开发/验收全在 train 侧候选(判准冻结:大小类 bad 旗标率 − good 旗标率 ≥ 40pt 且
good 旗标率 ≤ 15%),过门才并入否决门;eval 锚不参与任何迭代。

**E50 终判(2026-08-11):规则否决门(眉毛轴)eval 单发 = ev@95 从 29.13% → 30.55%(+1.42pt)**。
第 11 次 eval 接触。B2 于发车前判死(train 候选:大小类 bad 旗标率 33.3% vs good 30.8%,
差 2.6pt << 40pt 判准,good 假阳性 30.8% >> 15%——首末帧并排仍无分离,VLM 尺寸比较能力不足,
B 轴彻底关闭),故最终门 = 眉毛轴单独,构成于读数前冻结。
单发明细:否决集 eval 内 65 条(历史∪新确认);拦下尾部 bad 三条 = 5719(p=0.110)/
6565(0.174)/6228(0.216),阈值 0.2259→0.2397;代价 = 5805(normal, p=0.131)与
20074(good, p=0.022)两条被否决的可放行样本;净 +1.42pt。
ev@90 持平 0.4334(该门只在紧口径起作用);ev@100 微降 0.0409→0.0391。
**这是 E18(29.13%)定格以来第一个真实 eval 增益,且每一条拦截自带人可核对的理由
(VLM 确认的眉毛帧号)。部署形态:视频先过眉毛确认链(CNN 预筛可选,VLM 头部放大确认),
命中即拦;模型阈值按 95% 总召回重标定。**
诚实声明随判决存档:①眉毛轴的选择受用户对 eval 难例审计启发(选择性偏差),但检测器
参数冻结自 train 侧开发、eval 全量扫描 label-blind、三条目标经用户亲验为 ground truth;
②eval 侧 VLM 精度 17/30=57%,低于 train 侧 80%,但误拦代价已在混合制记账中如实扣除;
③被否决的 5805(normal)经 VLM 判有眉毛——按分级标准「眉毛出现即 bad」,疑似标签噪声,
留待 good 侧标签交叉检验。
**剩余头寸**:运动族 11 条(僵硬/卡顿/少动)仍无解——特征线(E30)、VLM 事实线(E50-D)
均已收档;还原度类 5 条中已拿下 3 条眉毛,余 5822(尾巴,C 轴关闭)与衣服一致性 1 条。

**E51 预注册(2026-08-11,用户方向:僵硬≠四肢不动,而是「面部相对活动」与「整体软硬」)**。
**机理修正(为何 E30 光流特征天生受限)**:TUTU 是哑光毛毡,身体内部近乎无纹理,
光流在平坦区域盲(孔径问题)——E30 测的"内部形变"多为噪声。形变真正可见处:
①**轮廓**(软身体运动时边界起伏)②**面部**(黑豆眼/嘴是高对比区,活物眨眼张嘴)。
**试点结果(本地 919 子集,18 运动bad vs 60 good,方向反转后)**:
轮廓软度 soft_med AUC **0.782**(bad 形变仅 good 的 40%:0.0235 vs 0.0585);
面部活动 face_act **0.775**;face_act_p90 **0.776**(good 有眨眼/张嘴突发 35.2,bad 平坦 16.5);
运动条件软度 soft_when_move 0.754(动时也不形变=贴纸);对比 E30 最高 0.66、geo 轨迹 0.60。
测量定义:掩码(白底抠像阈值恢复)→逐对相似变换对齐(矩:质心+面积尺度+二阶矩方向)
→ 1-IoU 残差 = 非刚性形变;面部=phaseCorrelate 对齐后中心区 absdiff + 暗像素占比波动(眨眼)。
**E51 全语料计划**:箱上 sam3_cutouts 全量 4845 已就位;face 轴以 crops_v3 上半区替代
(与试点 face_crops 同概念,先验证一致性)。纯 CPU 并行。
判准(发车前冻结):①全语料 train 侧复现试点量级(运动族bad|good AUC≥0.70 任一轴);
②监督组合分(运动bad vs good, OOF)做否决门模拟,train 混合制 > 0.3218 且增益主要来自
拦截尾部 bad(非方差红利——须过随机否决对照:同规模随机否决集的增益分布上沿);
③过门才 eval 单发(第 12 次接触)。
**声明**:试点 n=18 且来自保真选择的 919 子集,存在选择偏差可能,全语料验证为第一关。

**E51 判准①中期读数(2026-08-11)**:全语料 4845 测量完成。
轮廓软度 soft_med:试点 0.782 → 全语料 MT|G **0.648**、全族 MB|G **0.683**(反向读)
——缩水但存活,仍为运动族最强单特征(对比 E30 光流最高 0.662),**单轴未达 0.70 判准**。
**面部轴的崩塌是代理测量伪影,非概念证伪**:全语料用 crops_v3 上半 45% 压缩代理(0.553),
试点用 SAM3 原生分辨率 face_crops(0.775)——代理≠真测量。
处置:extract_face.py(线F 遗产,SAM3 认脸 384px)全语料重提取已发车(约 2h,断点续跑),
完成后以**与试点完全同口径**重测面部活动。判准①的最终读数以真 face_crops 为准。

**E52/E53 预注册(2026-08-11,自主新想法批,运动族攻坚)**。此前教训:轮廓软度/面部活动
两方向由用户点出;本批为 agent 自主生成的六路假设,判准全部发车前冻结:
①**弹跳谱**(活物待机有节律弹跳):geo 轨迹 FFT——**已判死**:16 帧采样≈3.2fps,
奈奎斯特 1.6Hz,弹跳频段被混叠,全部特征≈0.5(最高 oscamp_cy 0.570)。
死因有信息:振荡类信号必须全帧率。
②**E52 卡顿指纹**(全帧率帧差序列:近零帧占比/尖峰比/双峰性):原始视频约150帧,
全帧+角色区双通道,128px 灰度,48 进程 CPU。判准:任一特征 MT|G AUC≥0.70。
③**E52 时间箭头**(活物动作"预备-爆发-缓冲"不对称,刚体漂移时间对称):能量峰
上升沿/下降沿长度差。同判准。
④**盖-茎滞后 v2**(掩码上下分区质心互相关,非 E30 的光流条带):**暂缓**——
毛绒摆动滞后约50-100ms,16帧掩码(300ms/帧)分辨不出,须全帧率掩码(成本高,列为后备)。
⑤**E53 VLM 成对活物感比较**:与 E4(绝对打分,已死 0.546)的本质区别=成对比较远易于
绝对判断(感知实验标准结论),本项目从未试过。40 运动尾部bad + 40 good(盲)× 3 固定
参照good(模型最有信心且运动量最高,确定性选取),2×4 八帧拼图对,位置交替防顺序偏置。
判准:运动bad 平均输票 − good 平均输票 ≥ 1.0(满分3)且 good 高输票率 ≤ 20%。
⑥**vjepa2 运动头**:箱上缓存 2908/4845 哈希文件覆盖不全,重算需 GPU 2h+,列为后备。

**E52 判决 + 僵硬分 v2/v3 否决模拟判决(2026-08-11)**:
E52 卡顿指纹:**两个新独立信号成立**——c_hf_ratio 0.659/0.663(运动bad角色区帧差高频
能量更高=抖动指纹)、c_acf1 反向 0.647/0.654(运动连贯性更低);时间箭头 asym 判死
(0.49-0.54);still_frac 全帧率下亦死(好视频也常静止,基率问题再次出现)。
**僵硬分 v2(全域监督组合,49 维:rigid15+geo8+stall14+soft5+face代理)**:
OOF AUC 全运动族|good **0.775**、尾部运动|good **0.720**——分类器成立,
**但否决模拟全线净负**(N=60~300 全部 Δ<0):头部被"明显僵硬 bad"(E18 本就拦得住)
与"看着僵硬的 good"占满,150 个否决中仅 2 条尾部 bad。**分类器与 E18 学到同一批易目标。**
**僵硬分 v3(放行区条件判别,E18<0.30 区内 88 运动bad vs 650 good)**:区内 OOF AUC 0.744,
尾部命中大幅提升(N=30→4条/N=100→10条),**但仍全线净负**(最好 N=30 Δ-0.0022):
区内 good:bad = 7.4:1,30% 头部精度意味着每拦 1 条尾部 bad 陪 2-3 条区内 good,
否决数学撑不平。**结论:特征式否决门对运动族关闭**——除非区内 AUC 达 0.85+
(对应头部精度 ~60%),现有特征族(五机理合并 0.744)距离尚远。
剩余三张牌:①真 face_act(试点 0.775,提取中)并入后重估区内 AUC;
②E53 VLM 成对比较(已排队);③新特征族入栈走加列路线(须扣 +1.8pt 噪声列红利,
E30 教训:单特征入栈曾死,但 soft/stall 两族从未入栈)。

**E54 + 附属判决(2026-08-11 晚,自主批二)**:
E54 新特征族入栈:56 新列(rigid/stall/soft/geo/size/tailpink)加入 [oof15|320],
5 种子均值 **0.3150 vs 基准 0.3155,零增益**,与噪声列对照 0.3143 同水平——
组合器对新特征族免疫(与 E30 死法一致),**入栈路线对本批新特征关闭**。
分组结构:ti2i2v 相邻编号标签一致率 0.549 vs 期望 0.505,组信号弱,不追。
单特征极端否决(区内 top-N):全负;**最优浓度 = c_hf_ratio top-40(9 条尾部/29 good)**,
若 E53 成对比较能做第二把钥匙滤掉 good,组合否决可能翻正——一切押 E53 判决。
E55 预注册(自一致性,dinov2 逐帧嵌入对本视频中位嵌入距离):目标=衣服变化(15051)/
穿模/突增物体这类"自我不一致"缺陷,视频级特征会平均掉单帧异常,此角度从未测过。已链上排队。

**E58 预注册(2026-08-11 深夜,用户方向:生命力而非卡顿;E57 死寂窗口按用户指示搁置)**。
生命力的物理签名,全部需全帧率(弹跳谱曾死于 16 帧混叠,全帧率未测过):
①节律(呼吸/弹跳 0.5-4Hz 带功率+自相关副峰)②重力弹道(cy 滑窗二次拟合 R²,活物跳跃
是抛物线、无生命漂移是匀速)③静止段微节律(活的静止 vs 死的静止)④升降不对称(重力下落快)。
实现:角色区 96×96 Farneback 全帧率光流 vy 序列。
判准(冻结):任一特征 MT|G AUC≥0.70(方向不限);≥0.65 可与轮廓软度/卡顿指纹组合复算
区内否决模拟(须过随机对照)。

**E58 + 僵硬分 v4/v5/v6 终判(2026-08-12 凌晨)**:
**E58 生命节律 = 全项目运动族最强新特征族(用户方向:生命力,全帧率光流 vy)**:
grav_r2 重力弹道(活物跳跃是抛物线)反向 0.688/0.713;vzc 方向翻转率 0.684/0.669;
rhy_pow 节律带功率 反向 0.650/0.658;micro_rhy 静止微节律 反向 0.618。
**结构性发现(平台的解释)**:放行区 good 被 E18 构造性富集为静态 good
(区内 good 运动量中位 0.691 < 区外 0.726)——静=模型易确信,故运动特征在区内天然失效;
条件到"区内∩有运动"后 rhy_pow 升至反向 0.715、vzc 0.728,证实该机理。
**但否决经济学仍不通**:v4(13精选)0.694 / v5(58全量)0.733 / v6(条件化)0.733,
四种表述的区内 AUC 平台 0.69-0.74,veto 需 ~0.85;v6 最优点 N=10 恰打平(Δ=0.0000,
4 bad/6 good,小 N 精度 40%),其余全负。**运动族特征否决线以"结构边界已测实"收档**:
不是特征不够多,是放行区的对局(僵硬bad vs 静态good)在运动模态内不可分。
资产入库:liferhythm_full.csv(grav_r2/vzc/rhy_pow 为未来任何运动向工作的起点)。

**E59/E60 验证判决(2026-08-12 上午,新机 H100 NVL 96G)**:
迁移工程记录:S3 归档 crops_v3(08-05 版)eval 覆盖仅 13.5%——完整版随旧机器丢失;
用 sam3_feat geo 逐帧原始 bbox 从 mp4 重建角色裁剪(crops_geo,673/694,纯 CPU 秒级),
E60 门在新呈现下重验通过。视频目录名坑:S3 键 v-0430-* vs manifest 短名,软链解决。
**E60 悬空重建:过门**。重放一致率 83.7%(190/227,判准 ≥80%),新命中 32 vs 历史 33
——prompt 重建忠实。eval zone(e18<0.30,289 条,label-blind)扫描发车。
**E59 衣服首末对比:不过门,关闭**。衣服类 bad 旗标率 20.0%(n=55) vs good 6.7%(n=120),
差 13.3pt << 40pt 判准。死因:衣服变化多为中段瞬变或幅度细微,首末两帧对比召回不足;
good 侧 6.7% 误报倒是合格。该轴不入否决门,不迭代救(判准冻结)。

**E60 eval zone 终判(2026-08-12 中午):悬空轴否决经济学不通过,不入门,不烧第12次读数**。
zone 289 条扫描命中 23:**15 good / 4 normal / 4 bad**(4 bad 中 6565 眉毛门已拦、
5904/15932 在阈值上方、15088 恰在阈值边界)。代价 ~19×0.18≈3.4pt >> 收益 ≤0.9pt。
持续性过滤(persist≥3,prompt"持续悬浮"语义的机械执行,先经 train 重放验证)不改判:
train 一致率不变 83.7%,eval 仍 12 good/4 normal/3 bad——zone good 的误报是**持续型**
(雪地/白背景支撑面白上加白,VLM 系统性不可见)。
**门维持眉毛单轴,口径 30.55% 不变。**
**转化为复核资产**:若分级标准"悬空无支撑=bad"严格执行,这 16 条被判持续悬空的可放行样本
即标签复核头号候选;已并入眉毛 13 条 → relabel_candidates_v3.json 共 29 条(R01-R29),
配 gallery 交用户裁决。若用户确认改判,悬空轴的账将重算(被拦 bad 计召回而非代价)。

**用户标签裁决落账(2026-08-12 午)**:R03=20356(good→bad)/R14=15174(normal→bad)/
R27=6113(normal→bad)/R28=7219(good→bad);其余 25 条维持。诊断:眉毛误报主因=眯起的窄眼;
悬空误报=能飞场景或确无悬空。
**修正标签后的口径**:基准 0.2916;**眉毛门 0.3023(当前真实口径)**——20356(p=0.055)成为
眉毛门拦不住的漏检,口径自 0.3055 诚实回落;眉毛∪悬空门 0.2862(12 条误报代价仍压死收益,不入门)。
**E61 预注册(眉毛 prompt v2,窄眼防护)**:仅加强排除条款(E25 教训:不重写判断框架)——
显式描述眯眼形态 + 要求同帧同时可见眼睛与其上方分离线条。验证集全部有裁决依据:
must-keep = 三锚 + train 命中 40;must-drop = 用户维持原标注的 11 条误报。
判准(冻结):keep ≥90% 且 drop ≥60% 才替换生产 prompt 并重算门。
**E62 预注册(超参重扫)**:选择判据改 3 种子均值(E42 教训),90 随机配置;
晋级判准(冻结):3 种子均值 > 冠军同判据 + 0.010,过线才 eval 单发。
悬空+特征组合:正例仅 2 条(20356/15174),特征滤波器无法正当开发(n=2 拟合即 eval 过拟合),
暂记后备;若学长侧能提供更多悬空标注即可复活。

**用户修订 + E61/E61b 终判(2026-08-12 下午)**:
用户修订:20356 属边缘样本,撤回改判维持 good;最终改判 3 条(15174/6113/7219)。
**修正标签后官方口径 = 30.71%**(基准 0.2929;6113/7219 本为眉毛命中,改判后从误拦代价
转为召回信用,分母 563→560)。累计:29.13%(E18)→ 30.71%(眉毛门+标签修正)。
**E61(v2 整体重写)否决**:keep 2.8%,三锚全丢——"同帧同时可见眼睛+分离线条"的并存要求
实质重写了判断框架(真眉毛时眼睛常被挤压遮挡,无法确认并存即全弃)。
**E61b(二段式追问"是否眯眼")亦否决**:keep 0/36,三锚全部被"是眯眼"吞掉——
反向诱导:聚焦追问"是不是眼睛"使 VLM 对一切答是(E25 诱导定律的镜像)。
**结论:v2/v3 在相反方向各自灾难性崩溃,窄眼判别超出该模型 prompt 手术可达范围;
生产维持 v1(81-84% 精度),20074/5805 两条窄眼误报的 ~0.36pt 代价接受。**
工程记录:`cd X && setsid A & setsid B &` 中 B 不继承 cd(优先级),E62 首launch 跑错路径,
已用绝对路径重发(冠军 3 种子基线 0.3146,晋级线 0.3246)。

**眉毛门 v6.1(2026-08-12 下午):双钥匙恢复,官方口径 → 30.89%**。
诊断链:v6 prompt 本已含眼睛判别(81-84% 精度即其功);残差误报是感知层错误
(VLM 自称同时看见眼与分离弧线),文字不可修(v2/v3 反向双崩已证)。
**误报指纹:5/11 条误报 CNN bmax 彻底为零(0.000-0.030),5 条用户确认真眉毛全部 ≥0.096**
——两独立仪器矛盾即误报。v6.1 = VLM 确认 ∧ CNN bmax≥0.05:
恢复历史两把钥匙设计(eval 全量扫描曾绕过第一钥),0.05 远弱于历史预筛 0.5;
train 验证零损失(193 命中全 ≥0.1);三锚保留;20074(阈下代价项)消除。
口径演进:29.13(E18)→30.55(眉毛门)→30.71(标签修正)→**30.89(v6.1)**;
剩余误拦代价仅 5805 一条(CNN 0.93 的感知硬残差,接受)。

**E62 终判(2026-08-12 晚):✘ 未过晋级线,不烧 eval**。
90 随机配置 × 3 种子,冠军基线 0.3146,晋级线 0.3246;
最优 cfg50(leaves 5/est 260/lr 0.02/mcs 130/spw 1.07/ff 0.35/bf 0.99)均值 0.3189,
仅 +0.43pt < 可验证分辨率 +1.0pt(E42 定律:此量级 train 显著 ≈ eval 零)。
结论:E18 冠军参数已在该特征集的超参天花板附近,调参路线关闭。
明细存 box `data/hparam_sweep.csv`。
用户指令「另外你再仔细看能否调参提升放行率」至此收口:调参不可行,
本日实际提升全部来自门与标签侧(29.13% → 30.89%,+1.76pt)。

**E-API-1:Gemini pro 放行区否决门(2026-08-15)——判死,但信息存在性重大发现**。
设置:eval_v3 放行区(p_e18<T0=0.226,共 184=尾部 bad 20+gn 164),gemini-3.1-pro-preview
+官方标准 rubric+首帧参考,判 bad 即否决,眉毛门同款混合记账。
结果:**尾部 bad 召回 17/20(0.85)——特征系统结构性失明的那批运动/还原尾部,pro 看得见**;
但 gn 误拦 144/164(88%),净 ev@95 = 0.1607(-13.2pt),与眉毛门叠加 0.1661,判死不部署。
误拦理由横跨全轴(静止48/还原42/物体38/僵硬28),轴限制救不动(命中同族分布)。
机理:新旧标注标准冲突——放行区被 E18 构造性充满"静止的好视频"(旧标准容忍),
pro 按新 1233 标准(静止/平淡≤normal,出戏=bad)全部打死。非感知错误,是标准错位。
推论:①17/20 证明尾部判别信息存在且 VLM 可提取,"算法侧封顶"的历史判决限于特征通道;
②提取机制必须换:否决门在标准错位下不可用,可行路径=(a)pro 判决作为组合器特征入栈
(需 train 侧同批提取+重训+过训练门,是一场完整战役);(b)业务侧确认新标准是否取代旧标准
——若新标准为准,144 条"误拦"中相当部分按新口径本就该拦,eval_v3 口径本身需要重标。
成本:184 条×pro≈75 万 tokens。

**E-PORT-1:原部署系统(p_base/E18)搬运至新 1233 语料(2026-08-17)——完成,判决:老系统在新分布上近乎失效**。
工程:箱毁前抢救的 23 组特征缓存全部本地复原;X320 布局逆向并三重验证
(专家模型滑窗定位:cropped13=124-136 峰值0.565、mp[multipose20⊕mp50·150]=137-306 峰值0.659;
全320复现 eval_v3 ev@95=0.2913 与官方分毫不差)。模糊列 88-104(疑 asr/sft 混排)LGBM gain 占比
仅 0.4%,丢弃后 eval_v3 无损(B 配置:15专家+303列,ev@95=0.3055/br@80=0.5383)。
hint 专家消融(v1 替补风险检查):丢弃掉至 0.236/0.474——hint 有真信号,保留 v1 替补
(asr 缺失用全零≈无音轨向量,仅影响 hint/per_src_hint)。
结果(新 1233,bad 835/gn 398,新口径 br@80=固定放行 80% gn 看去除多少 bad):
p_base:AUC 0.5250,br@70/80/90 = 0.353/0.277/0.151;
E18-B:AUC 0.5308,br@70/80/90 = 0.363/0.267/0.157。
对照:同系统在旧 eval_v3 br@80=0.53-0.54 → 新分布上腰斩再腰斩;
dev150 同台:E18=p_base=0.176 < 纯运动电池 0.231 < pro 0.265 < motion+flash+pro 组合 0.315。
机理:与 E-API-1 同源——新标注标准(静止/平淡=bad、还原度从严)与旧标签驱动的特征栈错位;
且新语料生成链(李航奇图片插入 targets→i2v)与旧 0430 批零重叠,SKU 全换代。
推论:老系统不可直接搬运;新语料上的可用起点是运动电池+VLM 判官组合,
E18 分数可作为组合器一路特征(dev150 full-combo 0.315 已含 motion+flash+pro,下一步试并入 p_e18)。
产物:data/pbase/out/{scores_1233.csv, e18_B_model.pkl, X303_new.npy, pbase_1233.csv, full_raw_1233.npz}。

**[已撤回,见下方修正] F-R1/F-R2:新官方立绘 × flash(2026-08-17)——三变体全灭,参照图路线收档**。
素材:用户提供 Tutu2.0 官方五视角立绘(8 款 SKU,15290×8534 透明底+HTML 压缩版,
切分为 43 张视角图存 data/sku_ref_v2/views/)。dev150 终审(n=150,对照=v2 配置重跑):
对照 br@80=0.454/AUC 0.654;+3视角 0.417/0.622;+1视角(减干扰)0.454/0.632;
+3视角+逐部位清单 rubric 0.259/0.580。诊断:3 视角臂连还原度轴自身都降
(召回 0.538→0.487,族 AUC 0.630→0.596),运动轴不变——官方 3D 渲染风与生成画风的
gap 反而锚偏 flash,与 F1(旧 6 视角参照)同构。参照相似族第 7 次死亡,含 VLM 通道。
教训再证:pilot50(n=50)会骗人——+3视角在 pilot 上 br@80 翻倍(0.32→0.57),dev150 反转;
晋级判决一律 dev150 起步。
意外正资产:v2 配置 bad_score 直接排序在 dev150 br@80=0.454(历史档案 0.159 是 CV 组合器口径,
不可比)——flash 连续分本身已是组合器强特征候选。

**F-R1/F-R2 判决撤回(2026-08-17 14:4x)**:复核发现当日所有 flash 复跑(含对照臂)漏传
`--refs` 首帧参考图(历史 v2 每条带 ref:frame0,复跑 promptTokens 2290 vs 历史 3397),
且 rubric 仍写"第一张图片是首帧参考图"→ 立绘臂中模型把官方渲染图当成该视频首帧标准,
还原度判断被系统性污染。当日对照 0.454 与立绘臂 0.417/0.454/0.259 相互之间及与历史
均不可比,全部作废。修正版两臂(均带首帧 + 立绘臂顺序无关 rubric)已重跑,以新数为准。
教训入规:**复现历史配置先 diff 输出行的全部字段(ref/tokens),不许凭记忆搭配置。**

**R2-FINAL:新 1233 语料终版系统锁箱终枪(2026-08-17 21:5x)——br@80 = 0.5081,达成 0.5 目标**。
协议:组级 70/30 锁箱(dev 867/test 366,bad率 0.677/0.678 齐平);全部选型只在 dev
(三种子分组 10 折);底层件(X303-LGBM/15专家栈)dev-only 重训;test 单发一枪。
终版配置 D2:LR 层吃 [X303调参分(dev内OOF/dev→test) ⊕ flash两遍秩均值 ⊕ 轨内分位flash
⊕ track onehot ⊕ SKU onehot ⊕ 上游图片质检标签 ⊕ R1b/R1c 专家栈分]。
终枪:AUC 0.698 / br@70 0.617 / br@80 0.508 / br@90 0.222(dev 参考值 0.462±0.042,
test 落在区间上沿)。分解:exact-r2 0.681 最强,base-props 0.276 仍最弱;
族 AUC:大小 0.875/物理 0.727/还原 0.725/运动 0.606。
今日增量链:老系统搬运 0.24 → 键名修复+调参 0.32 → +flash(修正输入两遍均值) →
+track/SKU 条件化 → +图片质检标签(+8.6pt,最大单杠杆) → +专家栈 → 0.508。
当日判死清单:LoRA 7B×2/32B在途、参照相似×3(第7/8/9次)、prompt分工×2、pro排序
(两配置复现,bootstrap P(flash>pro)=93.3%)、gpt-5.2 静态抽帧(AUC 0.507)、
X303全宽结构(与堆叠无异)。
关键披露:①图片质检标签的部署可用性待业务确认(生产链图片质检是否先于视频生成);
②flash 单遍有 ±5pt 运行方差,部署应固定两遍均值;③标签一致性天花板疑在 AUC~0.7-0.75
(flash 自身两遍秩相关 0.71),标签复核是下一个解锁点。
产物:data/pbase/out/{lockbox_test_score.npy, r1_lockbox.npz, flash_*_1233.json};
锁箱切分 data/lockbox_split.json。

**E-32B:物理/运动专项 LoRA @ Qwen2.5-VL-32B fold0(2026-08-17 夜)——NO-GO,微调墙第 6 次**。
监督=物理/运动族(7子标签)bad vs gn,其他bad剔除;8帧输入;bf16+梯度检查点。
fold0(411 eval):族 AUC 0.5997 < 门 0.60,全bad 0.5046;分数压缩(std 0.032)。
至此微调路线总账:7B二分/7B还原度专项(带官方立绘)/32B物理运动专项 × 新旧语料
= 6 次独立失败,AUC 带 0.47-0.60。判决:千级样本+开源VLM微调对本任务整体收档,
判官通道唯一活路=闭源大杯零样本(flash 多遍均值)。fold1/2 取消。

## E-FREE-1 (2026-08-18): flash JSON 挖掘特征 + 工作点加权 meta —— 双死
- 背景:合法特征集新规(图片人工标注禁止作为推理输入;图片本身/图片侧模型分数合法)。
- 新 dev 合法基线 A1(X303⊕专家栈⊕flash双跑⊕track/SKU⊕图片探针2列, LR C=100 pw=2 标准化): br@80=0.4997 / br@70=0.622 / AUC=0.699(867 dev, 10-fold grouped, 3-seed)。
- flashmine 全量22列(reason onehot×15+grade×4+evidence+双跑一致+n_runs): br@80=0.4509(-4.9pt), AUC=0.7017(+0.25pt)——AUC 微升但工作点崩,判死。
- flashmine 瘦身5列(还原度/物理/不合理物体/evidence/一致性): br@80=0.4952(-0.5pt),判死。
- 工作点加权 meta(中段样本×2): br@80=0.4815(-1.8pt),判死。
- 结论:已有 flash 输出的边际信息已被 bad_score 双跑榨干;免费增益不存在,增量只能来自新信号(判图/refprobe/subjcons/prop/32B)。

## R3-DAY (2026-08-18 下午): 合法特征集攻坚记录(dev-only,锁箱已花)
存活链(8-seed 诚实口径): A1 0.500 → +newref官方图保真12列 → 基座重扫(346维,br@80选择) → 贪心剪枝(-TR_base-props,-r1c;新seed复核+0.9pt有效) → 基座bagging集成(2cfg×5seed秩平均) = **0.5287±0.0096**。
判死(全部有数): flashmine全量/瘦身、opw加权、per-track阈值、LGBM/blend meta、refprobe余弦、giant/dino/MIL探针(全≤0.61)、cropfid(0.420)、判图v1(AUC 0.4985)/v2意图条件化(0.5175/0.5222)、newref2十六帧giant版(0.490)、32B三折(族内0.530,第7次微调墙)、x303plus基座直换(P1 0.487)。
诊断锚点: A1漏网284 bad中还原度155(base-noprop 102/exact-r1 95);堵门gn仅56——主攻方向=还原度尾部。
关键教训: ①dev 867样本下meta每加一列都在br@80掏钱,信号必须以标量/基座折叠进入;②seed噪声±1.3pt,3-seed数字不可信,终读一律8-seed;③图片人工标注度量的轴与"可见还原度"不同(嵌入0.61 vs flash 0.52),VLM复刻图片标注此路不通。
在途: flash帧级判定(末帧vs源图+官方图,1233条)。

## E-VF-1 (2026-08-18): flash 帧级判定(末帧 vs 源图+官方图)—— 死
- 全量 1233,AUC=0.4888;还原度族内 0.4789(n=980)。818/1233 判 good。
- 与判图 v1(0.4985)/v2(0.5175) 三连:VLM 语义比对复刻不了"还原度"标签;newref 嵌入统计(+2.3pt)有效,说明该轴是分布性异常而非可言说的语义偏差。VLM-对照-参考路线今日封路。

## E-PX-1 (2026-08-18): 像素级开局漂移(frame0 vs 源图 SSIM/直方图)—— 死
- C2+pixdrift 8-seed 0.5224 vs C2 0.5362,-1.4pt。i2v 开局像素保真不载弹。

## R3-FINAL (2026-08-18 晚): 当日收官
- **终配 = C2-bagged**: 28列(r1b/r1c/flash双列/imgprobe_gbm/newref 11列/TR 4/SKU 8)+ LR(C=100,pw2)按 8 个 CV seed 装袋秩平均。
- **dev br@80 = 0.5417**(装袋口径;单seed均值 0.5362±0.0176)。当日增量 +4.2pt(合法基线 0.500 起)。
- 晚间判死: T3超参网格(复核seed现形)/T4 flash_skupct/T5秩变换/T7 newref2精选/T8 prop×轨交互/T2x16与软标签装袋(不再加分)/E1老模型迁移(-1.8)/probe2条件化探针迁移(换-5pt,双持零权重)。
- **图片侧独立战果**: 嵌入+SKU/dataset条件化探针,图片质检 AUC 0.610→0.6545 —— 可交付业务作图片质检自动化;dataset条件不可迁移至视频推理故不进视频组合器。
- **r1b′(专家栈折叠新特征)工程受阻**: 5次发车(fork池/faulthandler/SGD提速/线程池)均在 EXT 后静默死亡,无traceback,RAM充足;首次发车曾存活5h(11进程,LR 2513维过慢)。判定为箱内环境问题,方法未证伪 —— 下台机器 tmux 前台重跑,预期 +0-1.5pt。
- 8-seed 纪律确立: 3-seed 选型数字一律不作数(今天两次被复核 seed 打回)。

## R4 (2026-08-19):图片探针泄漏订正 + 隔离口径下的重新选型

**E-LEAK-1 图片探针的评估污染(本轮最重要的一条)**。源图探针训练自 4553 张已质检图片,
而 2.0 数据集 **1126/1233 = 91.3%** 的视频源图就在这批图片里,i2v 首帧≈源图,
全量拟合后给首帧打分等于给背过的样本打分。交叉证据:该探针在图片任务上的分组五折样本外
AUC 只有 0.610,而它给视频打分对视频标签的 AUC 有 0.6266——跨任务比本职样本外还准,只能是记忆。
隔离(图片池按源图五折,给某视频打分的模型不含该视频源图)后:探针 br@80 0.4106→**0.2487**、
AUC 0.6266→**0.5382**;融合 0.5417→**0.4378**。三种隔离构造互证:五折 0.2487 /
只用非 dev 组图片训练 0.2198 / 只用与 1233 完全无关的图片训练 0.2351;折内秩归一 0.2436
(排除折间尺度差)。**上线不受影响**(新源图不在训练池),受影响的是对成绩的估计。

**E-MAXCOS-1**:`refprobe_1233.csv` 的 p_lr/p_gbm 两列与 `imgprobe_1233.csv` 逐位相同,非新信息;
其中只有 `max_cos`(对官方视角图的余弦)干净。加入后 8seed 装袋 0.4378→**0.4429**(+0.5pt),
选型与复核种子方向一致,收。

**E-META-2 融合层重调**:去掉正类加权(pw 2→1)8seed 单种子 0.4344→0.4372、装袋 0.4429→**0.4566**;
C=100 与 C=1000 同分;对连续列做秩变换全负。

**E-HEAD-1 探针用哪个头**:图片任务 LR 0.6164 > LR+GBM 秩平均 0.6100 > MLP 0.5940 > GBM 0.5864;
视频侧 LR 列 0.2726/AUC 0.5551 > GBM 列 0.2487/0.5382。融合层单种子 0.4414 > 0.4372,
装袋反向(0.4532 vs 0.4566),按单种子均值取 LR 列。**一直在用的是差的那个头。**

**E-R1BP-1 r1b′ 判决:证伪(更正此前"工程受阻")**。专家栈折叠 EXT 63 列
(newref+subjcons+prop+newref2)在 A100 上跑通(约 1 小时,历史五次静默死亡是箱内环境问题)。
单路 r1c′ 0.3680/0.6194 优于旧 r1c 0.3526/0.6136,但进融合层:替换 **0.3986**、
四列并存 **0.4429**,均低于旧栈 0.4566。机理=折进去的信号与融合层已有的 newref 列重复,
专家栈原本提供的视角多样性被稀释。

**E-BLK-2 折叠必须嵌套(方法学订正)**:此前把块的折内子模型预测直接用于训练行,
训练行是样本内预测,融合层过度信任该列 → 系统性低估所有块,该批数字作废。
改成内五折样本外预测后重测:newref 全列 **+1.2pt**(唯一正),newref2 −0.2、subjcons −0.8、
phys32 −1.5、X303 原始 303 维 −2.2、prop −2.3、首帧嵌入(视频标签监督)−2.7。

**E-FULL-1 用全部 1233 训练**:锁箱 366 已开过一枪,继续把它排除在训练之外只是浪费数据。
dev867/10 折 0.4414±0.0117 → 全 1233/20 折 **0.4512±0.0083**(装袋 0.4551),dev 子集上 0.4634。
新口径下六个候选块再测仍全负(−0.4 ~ −1.5pt)。

**E-IMG-3 图片任务本身的天花板 ≈ 0.615**:emb 基线 0.610;LR 头 0.6164;
emb⊕按 token 去中心 0.6155–0.6168;good/bad 原型余弦 0.6042;同 token kNN 0.6009;
PCA 白化 0.5996。图片侧没有大空间,GBM 头那 2.4pt 是白丢的。

**E-NR3-1 newref3(形象还原轴加深,35 列)**:32 帧 × 官方视角曲线 + **跨款判别余量**
(本款最大余弦 − 其它款最大余弦)+ 最佳视角切换频率 + 首帧漂移曲线。
siglip2-base-224 编码器下:直接摊列 8seed 0.4157–0.4378、折成一列 0.4242–0.4355,
均低于基线 0.4414 → **该编码器下死**。注意边界:原始 newref(+2.3pt)用的是 so400m,
本轮 so400m 权重下载受阻未测,不构成对特征设计的干净证伪。

**E-SRC9-1 DINOv2 首帧结构漂移 = 本轮唯一有效新信号(+4.0pt,隔离口径)**。
`newref3.py` 的 `src_*`/`adj_*` 族:32 帧对**本视频首帧**的余弦漂移曲线统计
(均值/最小/首值/最大/落差/末段落差/斜率/低位占比)+ 相邻帧自相似最小值。
关键在编码器:**DINOv2-base 有效,siglip2-base-224 无效(−1.6pt)**——DINOv2 测结构对应,
siglip 测语义,而"角色越跑越不像自己的第一帧"本质是结构漂移。命中漏网 bad 里占 55% 的还原度轴。
12 列贪心剪枝到 9 列(剪掉 adj_mean/src_std/src_last)后四个读数全部一致向上:

| 读数 | 基线 | +src9 |
|---|---|---|
| dev867/10折 复核种子 | 0.4469 / 装袋 0.4617 | **0.4691 / 0.4821** |
| dev867/10折 8种子 | 0.4414 / 0.4532(AUC 0.6779) | **0.4621 / 0.4702**(AUC 0.6961) |
| 全1233/20折 复核种子 | 0.4483 / 0.4647 | **0.4635 / 0.4766** |
| 全1233/20折 8种子 | 0.4512 / 0.4551 | **0.4605 / 0.4778** |

**注意口径差异**:同一个 src9 在常规口径(含原探针)下不涨工作点(装袋 0.5417→0.5400),
只涨 AUC(0.7186→**0.7278**)。机理=泄漏探针已经吃掉了这部分方差(它知道源图标注,
而首帧结构漂移与源图质量强相关)。**常规口径 0.5417 已被自身伪影顶住,隔离口径才是能长的那条线。**

**E-NR4-1 patch 级结构漂移(newref4)**:DINOv2 patch token 对首帧做同位置余弦与软对应余弦
(每 patch 对首帧所有 patch 取最大,容忍位移),二者之差=位移量。31 列全上时两个口径打架
(全1233/20折单种子 0.4753 涨,dev867/10折 0.4557 降),典型列多样本少症状,剪枝中。

**E-NR4-2 patch 级漂移精简 4 列:两个口径同时过关,隔离口径装袋读数到 0.4974**。
贪心剪枝(31→28 列)在 dev867 口径掉分,改前向选择从 src9 出发只加 4 列——
`al_slope`/`al_p10`/`al_last`/`al10_min`,全部是**同位置** patch 余弦的统计量
(软对应那族一个都没进,说明有效信号是"原位形变"而非"位移")。

| 读数 | src9 | src9+patch4 |
|---|---|---|
| dev867/10折 复核种子 | 0.4691 / 装袋 0.4821 | 0.4747 / 装袋 **0.4906** |
| dev867/10折 8种子 | 0.4621 / 装袋 0.4702 | 0.4736 / 装袋 **0.4974** |
| 全1233/20折 复核种子 | 0.4635 / 装袋 0.4766 | **0.4743** / 装袋 0.4731 |
| 全1233/20折 8种子 | 0.4605 / 装袋 0.4778 | **0.4793** / 装袋 0.4862 |

单种子均值四个读数全涨,装袋四涨三。终配 42 列见 `data/pbase/final_cols_20260819.json`。
**R4 全天(隔离口径):0.4378 → 装袋 0.4974 / 单种子 0.4793,装袋 +6.0pt、单种子 +4.2pt。**
表述纪律:装袋是单次抽样,单种子均值才是可信统计量,对外应写"装袋读数 0.497,稳健读数约 0.475"。
